# Codex + Antigravity 轻量用量统计托盘

## 当前实现

项目已经建立为 .NET 8 WinForms 托盘程序，包含：

- 隐藏主窗口、NotifyIcon、右键菜单、Mutex 单实例和用户级开机启动。
- 使用简洁的几何图标作为主窗口、设置窗口和托盘图标。
- Codex 本地 session JSONL 扫描：支持 \`CODEX_HOME\`、默认目录、额外目录，按累计 token 做模型维度差分，单文件变更时安全重算并替换旧贡献。
- 统一 \`UsageBucket\`、日期/模型/项目聚合和 SQLite 本地缓存。
- 可编辑的 provider + model 价格规则，支持 input、cache read、cache write、output 四类单价；未知模型不回退到其他价格。
- 默认价格已覆盖 GPT-5.6 Sol/Terra/Luna、GPT-5.5、GPT-5.4 系列及 Gemini 3.7/3.6/3.5/3.1 等当前模型族；规则带有来源链接和核验日期。
- 主界面提供“获取最新价格”按钮，手动访问允许的官方 HTTPS 定价页；启动时不自动联网，抓取失败或无法识别时保留原有本地价格。
- 主界面按当前字体和 DPI 动态计算按钮、指标卡、图表坐标留白、状态栏、表头和数据行高度；窗口缩放时保持文字完整可读。
- 主窗口首次打开时读取并恢复上次正常关闭前的客户区宽高；用户完成拖拽缩放或关闭窗口时保存新的尺寸。
- 日期筛选支持今天、7 天、30 天、本月和“自定义…”；自定义项可选择本地日历的开始日期与结束日期。
- 设置窗口改为分行自适应布局，动态留出数字控件、长文本和按钮的显示空间。
- Antigravity JSON/JSONL 历史 token 发现和官方 status-line stdin recorder。recorder 按 conversation totals 去重、处理增量、检测 counter rewind，并记录模型、项目、transcript 路径和覆盖起始时间。
- Antigravity 历史解析支持 JSONL 逐行容错以及完整 JSON 对象/数组；当 JSON 历史没有可验证 token 时，按优先级以只读方式检查 SQLite 的显式 token 字段；未知 SQLite schema 和 PB 文件不会猜数字。
- Antigravity local quota 探测仅访问 loopback，并从本地 language server 启动参数读取 CSRF token，按接口要求发送 \`X-Codeium-Csrf-Token\`、\`Connect-Protocol-Version\` 和 metadata；解析 remaining fraction/reset，保存快照，离线时保留最后快照。
- API 等值采用主流 Sub2API token 口径：Input 显示未命中缓存输入，Cache Read 显示缓存读取，Cache Creation 显示缓存创建；本地仍保留原始总 Input 以便追溯。
- API 等值在 1x、token 计费模式下按 \`Input × inputPrice + Cache Read × cacheReadPrice + Cache Creation × cacheCreationPrice + Output × outputPrice\` 计算；不自动模拟 Sub2API 的用户/渠道倍率、priority/flex、按请求、图片或视频收费。
- 程序启动时在后台执行一次全量读取；普通手动刷新、运行中的自动刷新只检查新增或发生变化的文件。设置窗口提供“全量重新读取”按钮，供历史文件被外部重写或需要重新解析时使用。
- 刷新文件解析在后台执行；后台刷新完成后保持当前面板的日期和 Provider 选择，不再跳回默认日期。日期切换使用本地缓存快速整理。
- 托盘悬浮摘要同时显示 Antigravity 的 5 小时/周窗口；当 Codex session JSONL 包含上游返回的 \`rate_limits\` 时，也显示 Codex 的 5 小时/周额度和重置时间，否则明确显示额度未知。Codex 本地 session 日志始终显示 Input、Cache Read、Cache Creation、Output 和 API 等值。
- 托盘图标移动到上方时显示悬浮摘要；鼠标停止触发悬浮后 3 秒自动隐藏。左键单击托盘图标可固定/取消固定摘要，固定时窗口保持最顶层；点击摘要面板会关闭并取消固定。右键菜单打开期间暂停悬浮摘要，避免移动鼠标时菜单被顶层面板打断。
- 仪表盘按今天、7 天、30 天、本月和自定义区间切换，采用独立的高 DPI 布局区域，展示 API 等值、Input/Cached/Output、每日趋势、模型和项目表。
- 脱敏 fixture 与单元测试覆盖缓存计价、累计差分、status-line 去重/回退、完整 JSON 历史、只读 SQLite 历史、官方价格解析、quota 校验、额度摘要、未知价格降级、当前默认价格族和 Sub2API token 拆分。

## 可信度规则

API 等值只来自真实 token 或官方 status-line 的累计 token 差分；quota 百分比不参与美元换算。未知模型、价格缺失或 cache 拆分尚未验证时，金额显示为不可用，同时保留 token 数并提示未计价数量。一个汇总区间内只要存在未计价桶，整体成本质量不会被后续已计价桶覆盖。

价格更新只接受 OpenAI/Gemini 允许域名的 HTTPS 页面，并在解析到完整价格块后才写入本地 \`pricing.json\`；未识别规则不覆盖旧值。

本地来源的原始 \`Input\` 按包含缓存读取/创建的总输入保存；展示和计价时转换为 Sub2API 的三段式输入口径：未命中输入、缓存读取、缓存创建。这样不会把缓存读取按普通输入重复计费。

Sub2API 的标准 token 金额同样来自其网关记录的每次请求 usage 字段，而不是通过思维链或响应正文自行数 token。本项目在没有网关倍率、服务层级和非 token 计费信息时，按同样的 token 字段和公开价格计算透明 API 等值；因此同一份 usage、同一套价格下金额应一致。

本项目的 API 等值是公开 API 价格下的透明估算，不等于 Sub2API 的用户实际扣费。Sub2API 的倍率、服务层级和非 token 计费需要网关侧 usage log 才能准确还原。Codex session JSONL 在可用时还会保存上游返回的 \`rate_limits\`；本项目将其作为 Codex quota 快照解析。若当前 session 没有 \`rate_limits\`，则只显示本地日志用量，不把 token 数换算成订阅剩余比例。Codex app-server 还提供 \`account/rateLimits/read\` 这类实时读取能力，但不同 Codex 客户端/运行模式的本地可访问性不同，当前版本优先使用无需额外认证转发的 session 快照。

## 构建和验证

\`\`\`powershell
dotnet restore
dotnet test
dotnet build -c Release
\`\`\`

status-line recorder 可通过 \`tools/AntigravityStatusRecorder\` 独立调用，也可使用主程序的 \`--status-recorder\` 参数。

## 更新日志

- 2026-08-19：按 V2 API Equivalent 规格从零建立 WinForms 托盘程序、Codex 增量统计、统一价格/成本层、Antigravity 历史与 status-line token 链路、local quota 解析、SQLite 缓存、仪表盘和验收测试。
- 2026-08-19：补齐完整 JSON 历史文件解析，修正混合计价聚合的可信度传播，更新 GPT-5.6/GPT-5.4 与 Gemini 3.x 默认价格规则，并新增 3 个回归测试。
- 2026-08-19：增加只读 SQLite 显式 token 字段解析和来源优先级测试；无法确认 schema 时继续保守降级。
- 2026-08-19：重构高 DPI 主界面布局，增加官方价格页手动更新按钮、解析保护和 2 个价格更新回归测试。
- 2026-08-19：修复高 DPI 下按钮、指标卡、图表标签、表头、数据行和状态栏的文字裁切，改为按实际字体高度动态留白和自动行高。
- 2026-08-19：修复设置窗口高 DPI 裁切，增加自定义日期区间、托盘 5 小时/周额度及重置时间悬浮摘要，并更换为简洁几何图标。
- 2026-08-19：修复 Antigravity quota 因缺少本地 CSRF token 导致的空显示；增加独立的“未命中缓存读取”统计行及回归测试。
- 2026-08-19：将 API 等值的展示和输入拆分对齐 Sub2API token 口径，增加 Cache Creation 展示及兼容性回归测试。
- 2026-08-19：托盘悬浮摘要增加 Codex 日志用量和订阅窗口未知状态；右键菜单期间暂停悬浮层，自动隐藏改为 3 秒，增加左键固定/取消固定与点击关闭，并记忆主窗口尺寸。
- 2026-08-19：普通刷新改为增量模式，启动后台全量读取，设置中增加全量重新读取入口；刷新与范围整理移出 UI 线程并保持当前日期/Provider 选择；解析 Codex session 中可用的 \`rate_limits\`，显示 5 小时/周额度快照。


