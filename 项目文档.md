# AI Usage Tray (Codex + Antigravity 本地用量统计与额度监控)

AI Usage Tray 是一个基于 .NET 8 WinForms 的轻量级 Windows 本地系统托盘应用。它自动化读取本地 Codex 会话（JSONL）与 Antigravity（SQLite / Protobuf wire 解码 / Status-line）的真实元数据与 Token 消耗，按四类 Token 和固定非促销基准计算订阅消耗参考金额，并监控各模型池的 5 小时 / 周额度。金额用于比较消耗，不代表实际账单、订阅余额或官方固定兑换额度。

---

## 1. 文档导航体系

本项目遵循标准化文档规范，各文档职责如下：

- **[`项目文档.md`](file:///F:/Project/QuotaStatistics/项目文档.md)**（本文档）：系统技术总览、核心功能架构、数据源与计费口径、可信度规则与构建说明。
- **[`CODE_INDEX.md`](file:///F:/Project/QuotaStatistics/CODE_INDEX.md)**：代码总框架与模块索引，涵盖工程结构、类职责清单、调用时序图与维护扩展准则。
- **[`历史记录.md`](file:///F:/Project/QuotaStatistics/历史记录.md)**：详细版本演进历史，记录每次功能变更、缺陷修复与性能优化的具体时间与技术细节。
- **[`AGENTS.md`](file:///F:/Project/QuotaStatistics/AGENTS.md)**：AI 智能体协作与规范指令。

---

## 2. 核心功能与特性

### 2.1 真实数据采集与容错解析
- **Codex 本地会话解析**：解析器版本 11。读取权威 `token_count` 累计与请求级用量，过滤 `token_usage_record`、`compacted` 等重复或上下文记录；保留 Fast 档位、缓存分拆和长上下文信息。版本升级会重新解析旧缓存。
- **Codex 模型池识别**：显式池 ID 优先；`codex_bengalfox` 属于 Spark，普通 `gpt-5.3-codex` 属于主力。兼容真实旧日志中 `gpt-reserve` + `limit_id=codex` + 单独周 primary，以及 `base_model_inference`/`gpt-reserve` 格式。另外兼容真实 Pro/ProLite Spark 日志的通用 `codex` ID + Spark 模型 + 5h/周双窗口格式。主力、Spark、Reserve 分池展示。
- **窗口与时间状态**：服务器明确返回的 5h/周窗口按原样显示，不依据套餐名称隐藏。Reserve 在其自身有效周期内保留；不借用其他池的重置时间。每个池展示采样时间；15 分钟前的快照标为旧快照。超过重置时间显示“待同步（上次 …）”，等待服务器新快照。

- **Antigravity 原生 Protobuf Wire 解码**: 直接读取 `conversations/*.db` 的 `gen_metadata` 与 `steps`，通过零分配 `ProtobufSpanReader`（`ReadOnlySpan<byte>`）解析未命中输入、缓存读取、缓存创建、思考输出及响应输出，满足 `aggregate_output == thinking + response` 恒等式。
- **Antigravity 项目路径智能映射**: 结合 `conversation_summaries.db` 与 `trajectory_metadata_blob` 解析物理项目绝对路径，支持按项目（Project）维度统计。
- **本地实时配额探测**: 基于 PID 毫秒级探测本地 `language_server` 端口，通过 loopback 发送 CSRF Token 获取 `RetrieveUserQuotaSummary`，支持 Gemini Models 与 Claude / GPT 双模型池的 5h / 周额度解析。

### 2.2 订阅参考金额、Token 处理速率与周额度外推
- **Sub2API 三段式口径**:
  - **未命中输入 (Input)**: 实际消耗的未命中缓存输入 Token。
  - **缓存读取 (Cache Read)**: 命中的上下文缓存读取 Token。
  - **缓存创建 (Cache Creation / Write)**: 写入并建立上下文缓存的 Token。
  - **输出 (Output / Thinking)**: 包含思考链与正式响应的生成 Token。
- **Token 处理速率反推（Prefill 与输出速率）**:
  - **未命中 Prefill 速度（Cold Prefill）**: 针对未命中输入，统计冷启动 Prompt 矩阵计算的每秒输入吞吐（tokens/s）。
  - **命中 Prefill 速度（Cache Read）**: 针对命中缓存的输入，统计 KV Cache 检索与加载的高速吞吐（tokens/s）。
  - **输出生成速度（Generation Decoding）**: 统计模型自回归逐字生成（包含 Thinking 思考过程与响应正文）的平均输出速率（tokens/s）。
  - **统一格式呈现**: 同时展示“未命中”、“命中”与“输出”速率（如 `未命中 ~34.6k/s · 命中 ~125.0k/s · 输出 ~80.2/s`），并在设置窗口中提供原理与局限性说明。
- **订阅参考金额**：用户选定固定非促销基准，四段金额按 `(未命中输入 × 输入基准 + 缓存读取 × 读取基准 + 缓存创建 × 创建基准 + 输出 × 输出基准) / 1,000,000` 求和。缓存读取、缓存创建是总输入的子集，输出包含思考用量，避免重复计数。按单次请求应用长上下文阈值，不能以整日累计输入判定。
- **参考表版本 5**：GPT-5.6 Sol 保留每百万 Token $5 / $0.5 / $6.25 / $30，长上下文 $10 / $1 / $12.5 / $45；GPT-6 Astra 标准 $10 / $1 / $12.5 / $50，长上下文 $20 / $2 / $25 / $75。Codex Fast 在 Astra、5.6、5.5 上采用订阅 2.5 倍，5.4 为 2 倍；短、长上下文及周统计均保留该倍率。`priority` 作为 Fast 日志别名；API Batch/Flex 不作为订阅计量档位。
- **其他已核对基准**：Claude Opus 4.6 常规输入/读取/创建/输出 $5 / $0.5 / $6.25 / $25（创建以 5 分钟缓存为参考假设）。Gemini 3.1 Pro 缓存读取为 $0.2，长上下文为 $0.4。Gemini 3.6/3.7/3.8 Flash 选用官方列示的常规输入/读取/输出 $1.5 / $0.15 / $7.5，不使用截至 2026-12-31 的推广折扣。价格查看器列出每条规则的参考依据。
- **参考映射与未定价规则**：Reserve、Auto-Review 按用户原有约定使用 GPT-5.6 Luna 参考价（$0.2 / $0.02 / $0.25 / $1.2），缓存、长上下文和 Fast 同 Luna；该映射是参考计量约定。Spark 及其他未核实的泛化别名保留 Token，金额显示 `—`。用户可在本地价格文件配置其自定义参考规则；软件升级仅替换与历史内置规则完整一致的条目，保留自定义金额、长上下文及档位配置。
- **各池当前周**：主力、Spark、Reserve、Gemini、Claude/GPT 分别按自身 `[ResetAt−7天, ResetAt)` 查询。没有有效周期的池不纳入本次周统计；不使用日期汇总、其他池窗口或全历史作为替代。含未定价用量时，总金额不显示已定价部分的假总额。
- **满额样本外推与分模型多估算体系**：
  - **全池综合外推**：比较同池、同重置周期、同套餐的真实快照：`满额参考金额 = 两次采样之间的本机参考金额 / (初始剩余比例 − 末次剩余比例)`。区间为 `(初始采样, 末次采样]`；至少观察到 5 个百分点下降。
  - **Codex 分模型独立测算体系**：针对不同模型对 Codex 订阅额度的消耗比例与 API 费率比例不一致的客观现实（如 Astra 存在额外扣减惩罚，周满额折合金额显著低于 Sol），采用快照微区间切片归因算法。统计同周期与历史同套餐周期内各模型主导会话切片的额度下降与对应参考金额，外推各模型独立的每周总金额体系（如纯 Astra 测算周满额 vs 纯 Sol 测算周满额）。
- **订阅参考金额**: 用户选定固定非促销基准，四段金额按 `(未命中输入 × 输入基准 + 缓存读取 × 读取基准 + 缓存创建 × 创建基准 + 输出 × 输出基准) / 1,000,000` 求和。
- **参考表版本 5**: GPT-5.6 Sol 保留每百万 Token $5 / $0.5 / $6.25 / $30，长上下文 $10 / $1 / $12.5 / $45；GPT-6 Astra 标准 $10 / $1 / $12.5 / $50，长上下文 $20 / $2 / $25 / $75。Codex Fast 在 Astra、5.6、5.5 上采用订阅 2.5 倍，5.4 为 2 倍。
- **参考依据**：[Codex Fast 订阅规则](https://learn.chatgpt.com/docs/agent-configuration/speed)、[Codex 额度与 Token rates](https://learn.chatgpt.com/docs/pricing)、[Google 定价](https://ai.google.dev/gemini-api/docs/pricing)、[Opus 4.6](https://platform.claude.com/docs/en/models/opus-4-6/overview)。API 定价只是参考基准来源之一，不是订阅实际扣减规则。
- **多维度清晰展示与三处入口单源事实**:
  - **单源事实架构**: 系统严格确保**悬浮显示（托盘 Tooltip）**、**点击面板（悬浮窗 QuotaPopupForm）**与**双击主面板（MainForm 额度 Tab 与顶部卡片）** 100% 引用同一个源头，杜绝各处各自提取产生差异；
  - **悬浮显示 (Tooltip)**: 统一解耦标准模型与 Spark 模型，精确呈现 `AG 5h 87% / 周 70% | Codex 98% / Spark 77%`；
  - **点击面板 (QuotaPopupForm)**: 彻底消除 600+ 行重复自绘代码，重构为直接承载 `QuotaSummaryControl`；
  - **主界面卡片与周期**: 顶部卡片展示 `Codex: $主力通用 | $GPT-5.3 Spark`（如有 Reserve 自动并列显示）及 `Antigravity: $Gemini | $Claude`，周期栏对齐呈现多通道各自的到期重置时间；
  - **订阅额度与双周周期解耦**: 彻底将主力通用模型额度（标准周额度）与 Spark / bengalfox 辅助模型额度（Spark 周额度与 5小时额度）彻底双通道解耦；结合 Reserve 动态触发支持三通道并列统计。
  - **满额预估平滑保留与待更新标注**: 满额样本外推在快照未更新（距离上次请求超过 15 分钟）或新周期刚开始消耗不足 5% 时，不再隐藏金额仅显示纯文本，而是平滑保留推算金额（如 `约 $601.20`），并清晰标注 `（待更新）`；在周期重置初期自动继承上一已完成同套餐周期的测算基准。
  - **分模型周满额测算体系**: 针对不同模型在订阅中消耗扣减比例与 API 价格不一致的问题，利用微区间切片归因算法自动识别单模型主导切片，单独外推各模型的每周总金额，并在主面板模型表格与额度 Tab 中展示。
  - 额度面板与悬浮窗同步呈现分池额度百分比、倒计时与周满额预估。

### 2.3 现代化界面与极致交互
- **自适应高 DPI 渲染与栏位宽度记忆**: 主界面、设置窗口及弹窗均采用 `AutoScaleMode.Font` 与动态文本测量，杜绝高分屏下的文字截断；主界面表格（「按模型」与「按项目」）支持自由拖拽拉伸各栏位宽度，并与窗口尺寸（`WindowGeometryPersistence`）联动自动持久化保存，退出后重启依然保持一致。
- **专属高清图标**: 内嵌 16x16 至 256x256 完整尺寸的抗锯齿透明图层原生 ICO，统一应用于可执行文件、窗口标题栏与系统托盘。
- **高清自绘额度控件 (`QuotaSummaryControl`)**: 统一主窗口额度 Tab 与单击托盘弹出的悬浮窗（`QuotaPopupForm`），支持拖拽移动锁定、失焦防误关及单击/双击防冲突分流；点击面板内容或按 Esc 可主动关闭（固定显示时也可关闭），拖动松手保持显示、再次点击关闭；弹窗不超出屏幕工作区，长内容可滚动查看；重置时间均支持直观的“X天X小时X分后”相对倒计时显示，且智能处理过期周期的状态展示与同步引导。
- **丰富维度筛选**: 支持「本次周额度」、「今天」、「7天」、「30天」、「本月」、「全部」及「自定义起止日期」；顶部订阅参考金额卡片分应用（Codex / Antigravity Gemini & Claude）独立列出金额与有效周期。

### 2.4 Codex 历史周额度周期主面板页面
- **独立主面板页面**: 在主窗口底部的 TabControl 中新增专用的「Codex 周历史」Tab 页（`CodexHistoryControl`），完整展示自 2026 年 5 月至今本地 SQLite 数据库中记录的历次周额度重置周期。
- **全量快照变动时序捕获 (ParserVersion 12)**:
  - 彻底改变以往每个会话文件仅向数据库存储最后一行动作的覆盖机制，通过增量与变动去重机制（首尾快照必存、剩余比例变动 $\ge 0.5\%$ 必存、重置时刻改变必存、时隔 $\ge 2$ 小时必存），将数万条历史配额序列精简提炼为上万条精准的时序快照，杜绝长会话或跨天任务丢弃中间 100% 起始快照的问题。
- **周期聚类与非固定 7 天动态重置重构**:
  - **消除 API 网关抖动与并发交替碎裂**: 对同一模型池内的快照以 `ResetAt` 为基准并以 12 小时容差聚合为一个真实周期，并在簇内严格按抓取时间重排，彻底消除因服务器秒级时钟漂移或同一天不同任务会话交替输出导致同一周被撕裂成数十个碎片微周期的问题；
  - **严格单活跃周期约束**: 对于任意模型池，时序上唯有最新的最后一个周期且到期时间在未来时才标注为「● 进行中」；一旦发生重置并记录了后续新周期，此前所有历史周期全部归入「已重置」，杜绝多重「进行中」错乱；
  - **提前重置精准截断与起点推导**: 当用户套餐发生变动、手动重置或系统提前重置时，新周期的起始时刻以自身到期时刻倒推（`ResetAt - 7天` 或首个快照时间）为准，绝不错误继承上一周期在未来的失效计划到期日；同时旧周期的实际结束时间以新周期开始时刻截断，并在界面与详情中准确记录实际时长（如 1.0天、2.1天、3.8天等非整7天周期）及提前重置时刻。
- **核心数据与视觉呈现**:
  - **状态区分**: 「● 进行中」（深绿粗体高亮）与「已重置」（常规灰字）清晰区分；
  - **100% 完整周期额度基准与指标呈现**: 额度变化统一以官方周期的物理起点标准呈现为 `100% → {最低剩余%}`（如 `100% → 6%`、`100% → 0%`），已消耗按全周期基准核算（`100% - 最低剩余%`）；若实测首采快照略有滞后，在单元格 Tooltip 中完整展示周期额度基准、首次采样快照、最低剩余与实际消耗；并呈现模型池（主力/Spark/Reserve）、精确周期区间、实际时长、订阅参考金额、周满额样本预估、**模型独立测算**（展示各模型测算简写与富文本 Tooltip 详细展开）、产生总 Tokens 及采样快照数；
- **模型池快速筛选与底部明细探查**:
  - 顶部工具栏支持按「全部」、「Codex 主力模型」、「GPT-5.3 Spark」、「Codex Reserve」多通道筛选，并动态统计周期总数与进行中数量；
  - 选中列表中的任意周期，底部详情面板即时展现该周期的精确起止时间、采样快照覆盖时刻、未命中输入/缓存读取/缓存写入/输出 Token 消耗明细、综合满额预估说明，以及**第 4 行专属呈现该历史周期内各模型的独立周满额测算明细**；
- **高性能切片与自适应高 DPI 布局持久化**:
  - 采用一次性归一化事件内存切片与聚合算法，数十个历史周期秒开，无卡顿；
  - 深度适配 200%~250% 高 DPI（如 4K 分辨率）环境：表头高度、数据行高、顶部筛选条及底部 4 行详情面板均依据当前字体和 DPI 缩放系数动态推导与绝对像素布局，杜绝子控件重叠与截断；
  - 表格栏位支持智能安全防挤压保护（`minSafeWidth`），能自动识别并升级低 DPI 或历史配置文件遗留的被压缩过小宽度；支持用户自由拖拽拉伸并联动 `WindowGeometryPersistence` 与 `AppSettings.CodexHistoryColumnWidths` 自动持久化保存。

### 2.5 性能与低内存常驻架构
- **启动与刷新优化**: 启动默认增量比对（毫秒级）；仅在距上次全量扫描超过 7 天时在后台静默执行全量校准。
- **Codex 0 变更短路与 LOH 瘦身**: 无文件变更时跳过归一化重排；移除全量快照大 JSON 存库，消除 LOH 堆膨胀。
- **内存深度修剪 (`MemoryOptimizer`)**: 刷新后自动触发 LOH 压缩、GC 及 Windows 原生 `SetProcessWorkingSetSize`，后台常驻物理内存稳定保持在 15MB~30MB。
- **SQLite 缓存约束**: 配置 `PRAGMA cache_size = -2000`（限制 2MB 连接缓存）。

---

## 3. 可信度与隐私规则

1. **真实 Token 来源**: 订阅已用参考金额只来自可验证的真实 Token 或 status-line 差分。额度百分比不用于反推 Token；满额金额另以同周期样本外推并标明局限。
2. **保守降级与质量传播**: 未知模型或缺少价格规则时显示为 `—`，保留原始 Token 计数并在界面提示未计价数量，不静默归零或套用其他模型价格。
3. **零隐私上传**: 纯本地运行，不读取浏览器 Cookie、不要求 OAuth、不上传任何代码、提示词、响应正文或遥测数据。数据库存储于本地 `%LOCALAPPDATA%\UsageTray\usage.db`。
4. **参考基准保护**：价格更新入口合并随软件发布的已核对参考基准；不再抓取 API 页面覆盖订阅基准，避免促销价和模型别名误映射。

---

## 4. 构建、验证与发布

### 4.1 环境要求
- Windows 10 / 11 (x64 / ARM64)
- .NET 8.0 SDK

### 4.2 构建与测试命令

```powershell
# 还原依赖
dotnet restore

# 运行全量单元测试 (当前 171 项测试)
dotnet test

# Release 编译
dotnet build -c Release

# 运行主程序
dotnet run --project src/UsageTray/UsageTray.csproj
```

### 4.3 交付产物发布
每次代码修改与功能验证后，执行以下命令发布最新交付物至根目录 `publish/`。主项目明确设置 `PublishDir`，从旧的嵌套构建目录启动时也会将开机启动路径指向根目录交付物：

```powershell
& "C:\Users\xiong\.dotnet8\dotnet.exe" publish -c Release
```

---

## 5. 独立维护工具

- **Antigravity Status-line Recorder**:
  - 位于 [`tools/AntigravityStatusRecorder/`](file:///F:/Project/QuotaStatistics/tools/AntigravityStatusRecorder)
  - 可配置为官方 status-line 管道命令，实时监听并记录 stdin Token 流。
- **Codex Audit 工具**:
  - 位于 [`tools/CodexAudit/`](file:///F:/Project/QuotaStatistics/tools/CodexAudit)
  - 用于离线扫描 Codex 数据库并导出 CSV 审计报表。
