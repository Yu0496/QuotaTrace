# 历史记录与版本变更

本文档记录 QuotaTrace 项目从建立至今的所有功能迭代、问题修复、性能优化与规范演进记录。每次功能、规则、架构或说明内容变化时在此追加记录。

## 2026-09-11

### [RENAME/RELEASE] 项目正式定名 QuotaTrace：全系统、文档与 UI 品牌重命名与开源准备
- **修改时间**：2026-09-11 14:40:00（Asia/Hong_Kong）。
- **需求背景**：用户正式确定工具开源命名为 `QuotaTrace`，需对整个代码库的窗口标题、弹窗对话框、托盘提示、开源协议、中英文 README、项目总文档、代码索引及逆向文档完成全局重命名，并重新生成带最新标题栏的产品截图。
- **改动与实现详情**：
  1. **代码与 UI 重命名**：
     - `MainForm.cs`：主窗口标题在构造器与动态切语言事件中统一设置为 `QuotaTrace`；
     - `SettingsForm.cs`：设置窗口标题设置为 `QuotaTrace 设置`（英文 `QuotaTrace Settings`），弹窗 MessageBox 统一更新为 `QuotaTrace`；
     - `DateRangeDialog.cs`：日期范围验证弹窗标题更新为 `QuotaTrace`；
     - `QuotaDisplayFormatter.cs` & `TrayApplicationContext.cs`：托盘图标 Text 默认/兜底文字统一为 `QuotaTrace`；
     - `LICENSE`：版权所有者统一更名为 `Copyright (c) 2026 QuotaTrace contributors`。
  2. **文档体系全覆盖更新**：
     - `README.md` & `README_en.md`：标题升级为 `# 📊 QuotaTrace`，安装包名更新为 `QuotaTrace-v1.0.0-win-x64.zip`，Git 克隆路径更新为 `QuotaTrace.git`；
     - `项目文档.md` & `CODE_INDEX.md`：系统标题与总览说明全量更新为 `QuotaTrace`；
     - `codex_auto_review_reverse_engineering.md`：落地配置建议章节同步更新；
     - 采纳严谨的开源安全与网络表述：将定位文案统一细化为“零外网出站请求（No outbound requests from QuotaTrace）”，精确界定本地离线文件解析与 `127.0.0.1` 环回 IPC 通信边界，提升技术严谨度。
  3. **自动化测试与产品截图刷新**：
     - 执行 `ScreenshotGeneratorTests`，重新渲染主面板、托盘单击面板、托盘悬浮提示的中英文高清截图，确保截图中的窗口标题栏与托盘提示均为 `QuotaTrace`；
     - 仓库体积深度瘦身与资源格式优化：截图全面改用轻量高质量 JPG（85% 质量），将总推送体积从 17.7MB 骤降至数百 KB，极大优化 Git 克隆与网络传输速度。

### [FIX/UI & PRIVACY] 修复指标折叠与文字切断、README 悬浮交互截图纠偏、表格全景展示及全项目相对路径脱敏
- **修改时间**：2026-09-11 02:35:00（Asia/Hong_Kong）。
- **用户反馈**：
  1. 主面板缓存行数据仍然显示效果不佳，数字被错误折叠为 `3...`、`9...`，且下半部分字体被遮住；
  2. README 说明 1（悬浮窗）的截图错误地使用了 2（单击面板）的截图；3（主面板）的截图很多关键信息被遮挡，要求按实际使用的比例展示全部关键信息（参考上传的图 3）；
  3. 项目文档中的绝对路径改为相对路径，并检查全项目是否还有绝对路径与个人隐私泄露。
- **改动与修复详情**：
  1. **缓存指标折叠与字体切断修复 (`MainForm.cs`)**：
     - 根因定位：`InlineMetricLabel()` 误设了 `AutoSize = false` 与 `Font = 14F Bold`。在 `AutoSize` 列宽的 `TableLayoutPanel` 中，`AutoSize = false` 导致控件向布局引擎申报的尺寸塌陷，引发 `AutoEllipsis` 强制把 `31.72M` 与 `94.29%` 截断为 `3...` 与 `9...`；且 14pt 字体在紧凑行内导致数字基线与下半截被边缘物理裁切。
     - 修复方案：重构 `InlineMetricLabel()` 为 `AutoSize = true`、`Font = 10F Bold` 并附加 `Margin = (0, 0, 18, 0)`，行高增加至 `Math.Max(38, Font.Height + 20)`，文本垂直居中对齐。所有数字与百分比完全自适应实际文本宽度平铺展开，数字字形完整舒展，彻底消除截断。
  2. **README 截图纠偏与主面板全景展示 (`README.md`, `README_en.md`, `ScreenshotGeneratorTests.cs`)**：
     - **交互形态区分与截图纠偏**：明确系统托盘三层级渐进交互标准（1. 鼠标悬停 -> 托盘轻量 Tooltip 气泡；2. 鼠标单击 -> 弹出详细额度摘要 Popup 卡片；3. 双击 -> 完整主仪表盘）。在 `ScreenshotGeneratorTests` 中新增 Windows 任务栏高保真右下角悬浮提示生成器（`tray-tooltip.png`），清晰展示鼠标悬停在托盘图标上方时浮现的单行极简状态气泡；
     - **主面板全景无滚动条展示**：收敛日用量柱状图至优雅紧凑高度（~65px，大幅释放下方面板纵向空间）；将 `DefaultModelColumnWidths` 黄金比例重构为 `150/90/135/105/110/210/80/110/120`，测试生成窗口调整为 `1180 x 635`，使得 9 列表头（`模型`、`Provider`、`Input (未命中)`、`Cache Read`、`缓存命中率`、`预估速率`、`Output`、`订阅参考金额`、`推算周满额`）文字完全展开，5 行模型全部展示，彻底消除横向与纵向滚动条。
  3. **全项目绝对路径替换为相对路径与个人隐私脱敏**：
     - 批量替换 `项目文档.md`、`CODE_INDEX.md`、`历史记录.md` 中全部遗留的 `file:///F:/Project/QuotaStatistics/` 为标准 Markdown 相对路径；
     - 清理 `历史记录.md` 中遗留的本地硬编码发布路径 `F:\Project\QuotaStatistics\publish\`；
     - 清理 `AGENTS.md` 中硬编码的本地用户路径 `C:\Users\xiong`，改用通用环境变量 `$env:USERPROFILE`；
     - 全仓库执行隐私安全扫描（覆盖 `file:///`、`xiong`、`C:\Users`、`F:\`），确认全部敏感信息归零。
- **验证与交付**：
  - 全量 177 项单元测试 100% 通过；
  - 自动编译并执行 `dotnet publish -c Release`，顺利刷新 `publish/` 交付物并热启动常驻托盘程序。

### [FIX/DOC] 修复主界面速率截断与柱状图标题重叠、多语言细化与中英双语应用截图实测生成
- **修改时间**：2026-09-11 02:10:00（Asia/Hong_Kong）。
- **用户反馈**：
  1. 主面板 Prompt 缓存行与“每日 订阅参考金额”重叠且文字显示不全（速率被省略号截断、标题文字被切掉下半截），询问该行的含义；
  2. 询问 Markdown 中的图片链接如何上传到网页（是否直接提交 Git 即可）；
  3. 要求为 README 的三个面板说明补充中英双语的真实应用截图。
- **改动与修复详情**：
  1. **主面板布局重构与文字截断修复 (`MainForm.cs`)**：
     - 将柱状图容器重构为双行 `TableLayoutPanel`（第一行严格限制标题高度，第二行承载绘图画布），物理隔离绘图与文本，彻底消除标题被下方画布腰斩重叠的缺陷；
     - 重构缓存指标行（`cacheMissLine`）：将“未命中”、“缓存创建”、“命中率”从固定百分比改为 `AutoSize` 紧凑列，释放大量无用留白；为最右侧长文本“预估速率”提供 `Percent 100%` 充足宽度，彻底杜绝速率文本末尾省略号截断问题。
  2. **速率与额度摘要纯正多语言细化 (`TokenSpeedEstimate.cs` & `QuotaSummaryControl.cs`)**：
     - `TokenSpeedEstimate.ToShortDisplayString()` 接入 `I18n`，英文环境下正确显示 `Uncached ~.../s · Hit ~.../s · Output ~.../s`，不再残留中文字符；
     - `QuotaSummaryControl` 深度本地化：补充 `LocalizeEstimateNote`（如“样本消耗不足5%” -> “Drop < 5%”、“快照待更新” -> “Pending update”）及 `LocalizePoolName`（“Codex 主力模型” -> “Codex Primary”），中英切换百分百原汁原味。
  3. **双语高清晰界面截图生成与嵌入 (`docs/images/` & `README.md` & `README_en.md`)**：
     - 在 `docs/images/zh-CN/` 与 `docs/images/en-US/` 下生成了 6 张真实运行的高清晰无缝截图（桌面置顶悬浮窗、托盘单击轻量面板、完整主面板大盘）；
     - 分别在中文和英文 README 的三交互形态小节下居中嵌入对应截图。
- **验证与交付**：
  - 全量 177 项单元测试 100% 通过；
  - Release 发布包自动更新并重新拉起验证。

### [DOC/REFACTOR] 参照 MAA 规范重构 README、补充 3 界面交互说明、分立英文文档与修复控件事件泄漏
- **修改时间**：2026-09-11 01:05:00（Asia/Hong_Kong）。
- **需求背景**：
  1. 模仿 MaaAssistantArknights (MAA) 的 README 视觉与内容规范重构项目说明；
  2. 顶栏补充可选语言切换导航，拆分独立英文文档 `README_en.md`；
  3. 增加三种交互形态（桌面置顶悬浮窗、托盘单击轻量面板、双击主面板仪表盘）的场景与功能说明；
  4. 下载与安装前置并细分：普通用户 GitHub Releases 免安装下载即用，开发者源码构建发布。
- **改动详情**：
  1. **README 重构与双语分立 (`README.md` & `README_en.md`)**：
     - 采用 MAA 标准居中排版：项目标题、徽标集合（.NET 8、Windows 平台、MIT 协议、C# 语言）、顶栏 `[简体中文](README.md) | [English](README_en.md)` 导航行；
     - 详细梳理 3 种交互形态的功能定位、触发方式与核心价值：
       - **悬浮窗 / 桌面挂件 (Floating HUD)**：支持随意拖动置顶、进度条展示与精准倒计时；
       - **单击小面板 (Tray Mini Popup)**：即点即出不抢焦点，可随时拖拽转化为桌面常驻悬浮窗；
       - **主面板 / 仪表盘 (Main Dashboard)**：全景大盘、分模型周满额测算、KV Cache 缓存命中深度分析（按模型与按项目）、明细切片审计与系统设置；
     - 下载与安装优化：清晰区分普通用户免安装 Release 绿色版（下载解压双击即可运行，无注册表残留）与开发者源码调试与发布命令；
     - 保留并突出了核心功能与原理解析（分模型推算原因、Prompt 缓存优化、增量扫描与本地零网络隐私安全）。
  2. **组件事件解绑与销毁保护修复 (`CodexHistoryControl.cs`)**：
     - 重写 `Dispose(bool disposing)`，在控件销毁时显式从 `I18n.LanguageChanged` 静态事件解绑，杜绝跨测试/跨生命周期事件泄漏；
     - 在 `RefreshLocalizedTexts` 与 `ApplyFilter` 中增加 `IsDisposed || Disposing || _grid.IsDisposed || _grid.Columns.Count == 0` 严格保护，彻底消除无列时添加行的 `InvalidOperationException`。
- **验证与交付**：
  - 全量 176 项单元测试 100% 顺利通过；
  - 执行 `dotnet publish -c Release` 自动同步更新 `publish/` 交付物并重新唤起。

## 2026-09-10

### [PERF] 主面板异步独立加载重构、发布附属语言瘦身与 README 深度完善
- **修改时间**：2026-09-10 21:40:00（Asia/Hong_Kong）。
- **用户反馈**：
  1. 打开主面板时明显卡顿，希望让主页面和加载独立，避免因为加载导致软件卡住；
  2. 发布产物 `publish/` 存在多达十几种未定义语言的子文件夹，询问来源与优化方案；
  3. README 优化：把额度测算写在最前面并讲明分模型推算原因、增加模型/项目缓存命中率（Cache Hit Rate）说明、开头提供简短清晰的原理解释。
- **重构与优化详情**：
  1. **主面板异步解耦与渲染性能飞跃 (`MainForm.cs` & `PricingViewerForm.cs`)**：
     - 构造期与初次展示彻底解耦：移除构造函数中触发的三次冗余同步计算，窗口打开即刻 0 毫秒呈现 UI 骨架并立即渲染已有的内存快照；
     - 耗时计算异步化：将 `BuildSnapshot`（历史周期聚类、微切片切分、各模型周总额推算及速率统计）全部移入 `Task.Run` 后台执行，UI 线程 100% 保持流畅，支持拖动、缩放、切 Tab，彻底告别“未响应”；
     - 表格性能优化：将 `DataGridView` 的 `AutoSizeRowsMode.AllCells` 改为 `None`，杜绝数万次 GDI 文字尺寸度量惩罚；开启原生双缓冲（DoubleBuffered）与 `SuspendLayout` 批量渲染，表格刷新丝滑无闪烁；
     - 竞态序列保护：引入 `_loadSequence` 保证快速切换日期或筛选器时旧查询不会覆盖新查询。
  2. **发布产物语言瘦身 (`UsageTray.csproj`)**：
     - 在工程属性中配置 `<SatelliteResourceLanguages>en;zh-Hans</SatelliteResourceLanguages>`；
     - 彻底清除了来自 .NET 基础类库自动带出的 `cs`、`de`、`es`、`fr`、`it`、`ja`、`ko`、`pl`、`pt-BR`、`ru`、`tr`、`zh-Hant` 等 12 个冗余附属文件夹。
  3. **README.md 深度完善**：
     - 开头增加一句话极简原理解释（纯本地读取本地 CLI 会话日志与 Loopback 接口，零网络上传）；
     - 核心特性首位呈现【额度测算与整周满额预估】，并详述 Codex 按不同模型推算的原因（不同模型订阅消耗比例与 API 价格不成比例，Astra 等模型惩罚不同，切片归因避免混合计算失真）；
     - 增加【Prompt 缓存命中与优化分析】便捷功能介绍（按模型、按物理项目对比 Cache Hit Rate %）。
- **验证与交付**：
  - 全量 176 项单元测试 100% 通过；
  - 本地重新发布 Release 并拉起最新进程验证。

### [FEAT] 开源准备：中英双语国际化 (I18n)、Provider 独立启闭开关与开源规范化
- **修改时间**：2026-09-10 17:45:00（Asia/Hong_Kong）。
- **需求背景**：
  1. 准备将项目开源至 GitHub，提供完整的中英双语化（i18n）支持；
  2. 针对并非同时使用 Codex 和 Antigravity 的用户，增加独立的启用/禁用开关；
  3. 完善开源治理物料（中英双语 README、GitHub Actions CI 工作流、敏感数据清理与 .gitignore 强化）。
- **核心实现与改动详情**：
  1. **多语言国际化模块 (`UsageTray.App.I18n`)**：
     - 原生支持中文（`zh-CN`）、英文（`en-US`）及自动跟随系统语言（`auto`）；
     - 提供强类型 `I18n.T(zh, en)` 与 `I18n.Format` 格式化，并通过 `LanguageChanged` 事件通知界面实时刷新；
     - 全面覆盖主界面仪表盘、各 Tab 页面、表格表头（支持切换时动态更新）、悬浮窗（`QuotaPopupForm` / `QuotaSummaryControl`）、托盘菜单、设置面板（`SettingsForm`）、价格查看器（`PricingViewerForm`）、日期范围对话框（`DateRangeDialog`）以及相对时间/重置倒计时（`TimeFormatter`）。
  2. **Provider 独立启闭开关 (`EnableCodex` / `EnableAntigravity`)**：
     - 在 `AppSettings` 中增加 `EnableCodex` 与 `EnableAntigravity` 配置项（默认开启），并在 `Normalize()` 中约束至少保持一个开启；
     - `RefreshCoordinator` 调度短路：关闭的 Provider 彻底跳过磁盘会话扫描与本地端口探测，降低 I/O 开销与常驻内存；
     - UI 自适应联动：托盘 Tooltip、悬浮窗、主界面顶部卡片、下拉筛选框及 Codex 周历史 Tab 智能根据启闭状态隐藏或显示。
  3. **开源代码治理与安全脱敏**：
     - 从 Git 暂存中安全清理并反索引内部审计留档与本地大文件目录（`其他/`），确保用户本地分析文件安全且不进入 GitHub 仓库；
     - 扩展强化根目录 `.gitignore`，涵盖 Visual Studio/Rider 缓存、发布物料、测试输出、临时日志与本地审计数据；
     - 新增 `.github/workflows/ci.yml`，在 GitHub 托管的 `windows-latest` 环境下执行自动化 restore、build 与 test 流程；
     - 重新编写双语 `README.md`，提供清晰的项目介绍、双语功能清单、架构与本地隐私声明、快速上手指南。
- **验证与交付**：
  - 新增 `I18nAndProviderToggleTests` 单元测试，修复测试环境 UI Culture 兼容性，全量 176 项单元测试全部通过；
  - 自动执行本地 Release 发布（`dotnet publish -c Release`），更新 `publish/` 交付物。

## 2026-09-09

### [FIX] 彻底重构 Codex 会话快照变动采集机制与历史周期 100% 额度基准规范化
- **修改时间**：2026-09-09 21:25:00（Asia/Hong_Kong）。
- **用户反馈与疑问**：
  1. 历史周期的额度变化不是以完整额度周期计算的，很多开始都不是 100%，直接从 70%、80% 开始了，看起来像残缺周期或属于上个周期。一次完整的 100% 额度周期应该准确按每次额度来显示与核算。
  2. 用户强调只在本机单机使用 Codex，未在云端或其他设备使用，质疑为什么之前分析会说用量缺失；同时询问 session 归档是否会影响统计。
- **深入全量排查与根因确证**：
  1. **归档影响排查**：验证确认 `CodexSessionLocator` 本身就同时搜索 `~/.codex/sessions` 与 `~/.codex/archived_sessions`，归档文件 100% 正常解析。
  2. **“记录缺失”与“快照从 70%~80% 开始”的根本原因查明**：
     - 用户本地磁盘 446 个会话文件中完好无损地记录了多达 51,194 条配额返回数据，本地绝未丢失数据！
     - 真正根因在旧版 `CodexJsonlParser` 的快照缓存机制：解析单个会话文件时，后一行快照直接覆盖前一行。导致不论单个会话文件跨越多少天、调用了多少千次交互，**最终仅将文件最后一行的单条快照写入数据库，中途成百上千条快照（包括新周期刚开始时的 100% 快照）被全部丢弃**！
     - 比如 8 月 31 日 05:09 新周期重置为 100% 后的首行快照就是 100.0%，但因该会话持续到中午 11:10（额度降至 81%），旧代码覆盖丢弃后只录入了 81%，导致数据库中周期起点变为 81%；
     - 同时 8 月 27~28 日 607MB 文件中 838 次请求（消耗 1.03 亿 Token）调用了 `gpt-reserve`，OpenAI 当时记入了周额度，而旧聚合器硬编码将其剥离，引发“额度掉了 31% 但本地主力模型只有 $0.47”的假象。
  3. **并发会话导致历史周期被拆散的问题**：
     - 旧版 `UsageAggregator` 按抓取时序分组，且仅支持 2 小时容差。当同一天开辟了多个任务会话或服务器微小调整到期秒数时，交替输出导致同一周被拆碎成数十个微周期。
- **重构与系统级修复**：
  1. **`CodexJsonlParser.cs` 升级至 ParserVersion 12**：
     - 引入配额变动增量采集与去重机制：首尾必存、剩余变动 $\ge 0.5\%$ 必存、`resets_at` 改变必存、时隔 $\ge 2$ 小时必存；
     - 升级版本号自动触发全量重建，将 446 个会话文件的真实历史快照从 434 条补充恢复为 1.1 万条以上精准时序。
  2. **`UsageAggregator.cs` 周历史聚类算法重构**：
     - 快照按 `ResetAt` 以 12 小时容差聚类（周额度物理周期为 168 小时），簇内按抓取时间重排，彻底消除并发交替分裂，原本碎片化的记录完美融合成整洁的自然周。
  3. **`CodexHistoryControl.cs` 规范化 100% 周期额度基准展示**：
     - 「额度变化」列统一呈现为官方周期的标准物理基准 `100% → {最低剩余%}`；
     - 「已消耗」列基于 100% 完整额度准确核算（`100% - 最低剩余%`）；
     - 若实测首采快照略有滞后，在单元格 Hover Tooltip 中清晰呈现周期额度基准（100%）、首次采样快照、最低剩余与全周期实际消耗。
- **验证与交付**：
  - 新增 `CodexJsonlParser_PreservesIntermediateSnapshots_AndAggregatesWith100PercentBaseline` 单元测试，全量 171 项测试通过；
  - 自动执行本地 Release 发布（`dotnet publish -c Release`），更新 `publish/` 交付物。


### [FIX] 修复外部设备/云端消耗导致本机用量缺失时切片产生异常极低推算（如 $2.55）的问题
- **修改时间**：2026-09-09 20:59:00（Asia/Hong_Kong）。
- **用户反馈**：在历史周期详情中发现 `gpt-5.6-sol 约 $2.55 (实测18.4%) | codex-auto-review 约 $2.55 (实测12.6%)`，询问为什么会测出 $2.55。
- **根因分析**：
  1. 8月28日早晨至8月29日中午（间隔长达 29 小时），官方快照额度从 84% 骤降到 53%（下降了整整 31%）；
  2. 但在该期间，该 31% 的额度主要在其他电脑、网页端或云端环境消耗；本机在此 29 小时内仅在最后阶段产生了 8 次微量调用，总计金额仅 \$0.79（Sol \$0.47，Auto-review \$0.32）；
  3. 原切片算法缺少“切片合理性与跨设备样本缺失校验”，把这仅有的 \$0.79 本地微小费用，除以了在外部消耗的 31% 额度（\$0.79 / 31% = \$2.55），导致 Sol 和 Auto-review 双双得出荒谬的 \$2.55；
  4. 全池预估此前已有未定价防护因而显示为 `—`，但分模型测算未做切片合理性底线过滤。
- **重构与算法修正**：
  - 在 `QuotaProjector.EstimateModelProjections` 的切片构建与历史回溯中引入**“切片合理性底线校验”**：若切片折合周满额 `TotalCost / Drop < $15.00`，判定该切片属于“外部设备消耗导致本土样本缺失”，坚决予以剔除；
  - 剔除后该周期自动回溯继承可靠的同套餐历史基准（例如 Sol 恢复继承历史稳定的 \$132.46），彻底消除 \$2.55 异常数据。
- **验证与交付**：
  - 新增 `EstimateModelProjections_IgnoresSlices_WithMissingLocalUsage_DueToExternalDevice` 单元测试，全量 170 项测试全部通过；
  - 执行 Release 构建发布并重新拉起最新版本。

### [FIX] 修复分模型周满额微切片在中间快照额度未变时遗漏 Token 导致推算严重低估的问题
- **修改时间**：2026-09-09 20:43:00（Asia/Hong_Kong）。
- **用户反馈**：本周基本只用了 Astra，但主面板表格中 Astra 的推算周满额（$192.26）和总订阅额度推算（约 $362.68）相差巨大。
- **根因分析**：
  1. 用户本周总额度下降 17%（从 1.0 降至 0.83），主力模型本轮实际参考金额 $61.77，其中 `gpt-6-astra` 占 $60.08（97.3%）。
  2. 全池总预估按首尾两点计算：$\$61.77 / 17\% \approx \$362.68$（纯 Astra 应为 $\$60.08 / 17\% \approx \$353.41$）。
  3. 但在分模型独立测算中，原逻辑采用两两相邻快照步进。在用户密集调用 Astra（产生 $28.70 费用）期间，官方配额快照由于上报延迟或阶梯扣减，连续多个快照停留为 0.86（86%）。旧代码未发生下降时盲目推进 `previous`，导致这一个半小时内的所有会话 Token 掉入两快照之间的黑洞，被彻底丢弃。
  4. 最终切片仅统计到 $32.68 的 Astra 费用，却除以了全量的 17% 下降，得出 $\$32.68 / 17\% \approx \$192.26$，分子被腰斩导致严重低估。
- **重构与算法修正**：
  - 将分模型切片与历史回溯改为**“额度变动锚点（Anchor）”**机制：快照指针仅在额度发生真正下降时才结算区间所有 Token 并前移锚点；未变动的快照不推进锚点，确保期间所有会话 Token 100% 完整纳入下一次额度下降切片中。
  - 修复后 Astra 推算周满额立即纠正为 **$354.17**，与总额度预估 $362.68 高度契合（吻合度 97.6%）。
- **验证与交付**：
  - 新增 `EstimateModelProjections_RetainsUsage_AcrossIntermediateUnchangedSnapshots` 单元测试，全量 169 项测试全部通过；
  - 重新执行 Release 构建与发布，并在后台拉起最新交付产物。

### [FEAT] 满额外推陈旧平滑保留“待更新”与 Codex 周历史模型独立测算体系全面集成
- **修改时间**：2026-09-09 17:00:00（Asia/Hong_Kong）。
- **用户需求与反馈**：
  1. 满额外推即便需要更新（如超过 15 分钟无新请求导致快照 stale 或新周期初始样本不足），也应该保留并显示之前的推算金额，标注“待更新”即可，避免直接清空金额显示。
  2. 将分模型周满额测算体系（针对不同模型配额扣减比例与 API 价格比例不同而建立的独立测算）加入到「Codex 周历史」，支持回溯查看历史各周期的模型独立测算数据。
- **架构与算法设计**：
  1. **快照陈旧平滑保留金额 (`QuotaProjector.cs`, `QuotaSummaryControl.cs`, `QuotaDisplayFormatter.cs`)**：
     - `QuotaProjector.Estimate` 移除针对 `latest.IsStale(at)` 直接置空 `FullValue` 的逻辑。当本周期尚未重置但快照陈旧时，仍基于周期切片计算全额推算，`Note` 标记为 `"快照待更新"`；
     - 主面板额度 Tab、悬浮窗 `QuotaSummaryControl` 与托盘单行 Tooltip 遇到“快照待更新”时，均保留测算金额（如 `约 $601.20`），徽章与后缀平滑切换为 `（待更新）`，杜绝数字突然消失；
     - 当前周额度消耗不足 5% 无法推算时，自动回溯继承上一已完成周期的主模型测算与分模型测算结果，同样标记为 `（待更新）`。
  2. **周历史切片独立分模型测算 (`UsageAggregator.cs`, `UsageViews.cs`)**：
     - `CodexHistoricalCycleView` 扩充 `ModelProjections` 属性；
     - `BuildCodexHistoricalCycles` 为每个标准池历史周期调用 `QuotaProjector.EstimateModelProjections`，在各自历史周的时间窗口与快照切片内严格计算 Astra、Sol 等模型的独立主导测算；
     - 调整聚合构建时序，先构建历史周期列表，使当前进行中周期能在不足 5% 时无缝继承上一周期的历史基准测算。
  3. **周历史表格与详情排版全面升级 (`CodexHistoryControl.cs`)**：
     - 表格增加第 9 列「模型独立测算」（默认宽度 180px，支持 DPI 动态缩放与配置持久化保存）；
     - 单元格紧凑渲染各模型测算概览（例如 `astra: 约$601 | sol: 约$1,610`），Hover Tooltip 展开完整消耗占比、参考金额与数据来源；
     - 底部详情面板扩展为 4 行独立布局，新增青绿色 `_detailModelLabel`，选中任意历史周期即可直观查看其分模型测算明细。
- **验证与交付**：
  - 更新 `AccuracyRepairTests.cs`、`CodexModelQuotaProjectionTests.cs` 及 `CodexHistoricalCyclesTests.cs`；
  - 全量 168 项单元测试全部通过（0 失败，0 跳过）；
  - 自动执行本地 Release 发布（`dotnet publish -c Release`），更新本地交付目录 `publish/`。

### [FEAT] 新增 Codex 分模型周满额独立测算体系及主面板呈现
- **修改时间**：2026-09-09 13:54:00（Asia/Hong_Kong）。
- **用户需求与背景**：
  - 用户提供 ChatGPT 讨论分享，指出 Codex 不同模型对订阅配额的扣减比例与 API 官方价格比例并不一致（例如 `gpt-6-astra` 扣减配额显著快于 API 价格折算值，存在约 1.5~1.6x 的扣减惩罚，导致 Astra 测算出的周满额总价值显著低于 `gpt-5.6-sol`）。
  - 要求针对 Codex 的每个特定模型单独测算，以特定模型消耗的特定额度测算每周总金额，形成多个估算体系。
  - 界面范围明确限制：**只显示在主面板（MainForm）当中**，在主面板每个特定模型的后面加上该模型的推算周满额。
- **架构与算法设计**：
  1. **微区间切片归因与主导模型检测 (`QuotaProjector.EstimateModelProjections`)**：
     - 在同一周期内，遍历相邻快照对微切片，提取切片内各模型的 Token 参考金额；
     - 若切片内单模型金额占比 $\ge 70\%$，判定该切片为该模型的“主导区间”，将额度下降比例 $\Delta Q$ 与该模型参考金额累计至该模型；
     - 当累计额度下降达标（$\Delta Q \ge 2.5\%$）且无未定价 Token 时，通过 $\text{FullUsd} = \text{Cost} / \Delta Q$ 外推该模型主导下的全周总额。
  2. **历史同套餐周期主导样本回溯兜底**：
     - 当本周该模型尚未积累足够（$\ge 2.5\%$）的独立下降样本时，自动回溯历史同套餐（如同为 Pro 400x 或同为 Plus 20x）的已完成周期，继承其主导样本测算基准，并在详情中明确标注“（继承自历史同套餐样本）”。
  3. **数据模型与服务层契约升级**：
     - `UsageViews.cs`: 定义 `ModelQuotaProjectionView`（记录模型名、周满额、消耗比例、参考金额、详细说明及本周/历史标识）；`ModelUsageView` 增加 `EstimatedWeeklyCostUsd`、`EstimateNote`、`EstimateDetail`；`CodexCycleUsageView` 增加 `ModelProjections`；`DashboardSnapshot` 增加 `CodexModelProjections`；
     - `UsageAggregator.cs`: 主力通用池在 `BuildSingleCodexCycle` 时计算分模型测算并注入周期视图；在构建仪表盘时将分模型测算直接注入各模型的 `ModelUsageView`。
  4. **主面板专属呈现**：
     - **MainForm 模型表格**：增加第 7 列「推算周满额」（格式如 `约 $601.20 (实测17%)` 或 `约 $1,610.50 (历史基准)`），单元格 hover 绑定 Tooltip（展示详细计算过程、消耗配额与 API 价格）；并在窗口几何持久化配置中注册默认列宽（155px）；
     - **QuotaSummaryControl（主窗口额度 Tab）**：在主力通用池的综合满额预估下方，展开展示分模型测算树分支（例如 `├─ 按 gpt-6-astra 测算: 约 $601.20`），悬浮窗与托盘 Tooltip 保持原精简样式不增加干扰。
- **验证与交付**：
  - 新增 `tests/UsageTray.Tests/CodexModelQuotaProjectionTests.cs`（单模型切片测算、历史同套餐继承、聚合器端到端装配 3 项测试）；
  - 全量 165 项单元测试全部通过；
  - 自动执行本地 Release 发布（`dotnet publish -c Release`），更新本地交付目录 `publish/`。

### [FIX] 修复 Codex 周历史多重“进行中”、未来起始周期及秒级抖动拆分裂变重复条目问题
- **修改时间**：2026-09-09 03:02:16（Asia/Hong_Kong）。
- **用户反馈**：
  1. 怎么会有多个“进行中”？（主力模型出现 2 个，Spark 出现 3 个）
  2. 现在时间都没到 9 月 13 号，哪来的从 9 月 13 号开始的额度周期？
  3. 在一些模型（比如 Spark 模型）中，把很多重复区间的内容统计到了不同的条目，但它们明明是同一个区间内的。
- **根因分析**：
  1. **API 网关时钟与上报秒级抖动**：OpenAI 服务器集群在不同请求中上报的 `resets_at` 会产生 1~43 秒的微小抖动（例如 Spark 在 9月6~7日的快照分别上报了 05:57:35、05:57:39、05:58:04，主力模型也存在 04:48:54 与 04:48:55 的 1 秒抖动）。旧逻辑采用秒级精确 GroupBy，直接导致同一个 7 天周期被切碎成了 2~3 个重复条目；
  2. **提前重置与链式推算未来污染**：用户的主力模型与 Spark 额度在 9月8日发生了提前重置（恢复为 100% 额度并获得新到期日 9月15日），而上一周期的计划到期日原本是 9月13日。旧算法在计算 9月15日新周期时，盲目使用上一周期的预定截止时刻 `prevResetAt`（9月13日）作为新周期的起始时刻，导致新周期的区间变成了未来的 `09-13 ~ 09-15`。因为起始时刻处于未来，`now >= cycleStart` 判定为 false，反而被错误标记成了「已重置」；
  3. **缺乏全局单活跃周期约束**：旧逻辑对每个碎片周期只要 `resetAt > now && now >= cycleStart` 就标记为进行中，当同一周期的抖动碎片都在未来时，便出现了同一模型池同时显示 2~3 个「进行中」的混乱现象。
- **重构与算法修正**：
  1. **聚类消除秒级抖动与重复条目**：对同一模型池内的快照引入时间容差聚类（`|ResetAt - LastResetAt| < 2小时`），将 43 秒以内的网关抖动收束为同一个完整真实周期，彻底消除 Spark 及主力模型下的重复条目；
  2. **严格单活跃周期保障**：为每个模型池确立单一活跃周期法则，只有时序上最新的最后一个周期且到期时间在未来时才标注为「● 进行中」；一旦检测到已进入后续新周期，此前所有历史周期全部归入「已重置」，杜绝多个进行中；
  3. **提前重置精准截断与起点保护**：若上一周期的计划重置时间大于新周期的首个快照时间（即发生提前重置），新周期起始时间绝不引用未来的失效计划时刻，而是由其自身重置时间倒推 7 天（`resetAt - 7天` 或首个快照时刻）确定合法过去起点（2026-09-08 15:31）；同时上一周期的实际结束时间以新周期的起始时刻截断，并在界面与详情中准确记录实际时长（如 2天2时）与提前重置时刻。
- **验证与交付**：
  - 新增 2 项针对抖动聚类与提前重置单活跃约束的单元测试（`BuildCodexHistoricalCycles_ClustersJitterAndEliminatesDuplicates`、`BuildCodexHistoricalCycles_HandlesEarlyResetAndGuaranteesSingleActive`），全量 162 项测试全部通过；
  - 自动执行 Release 发布部署至根目录 `publish`，并在后台无缝拉起（PID: 10612）。

### [FIX] 修复 Codex 周历史在高 DPI / 4K 缩放下列宽被挤压截断与详情面板文字重叠问题
- **修改时间**：2026-09-09 02:40:22（Asia/Hong_Kong）。
- **用户反馈**：主面板「Codex 周历史」打开根本看不清（附图显示：高分辨率/高 DPI 下所有列宽被压缩至 30~40px，表头与内容全部被省略号截断成「状... 模... 周... 额... 已...」，底部详情面板 3 行文字互相践踏重叠在一起）。
- **根因分析**：
  1. 经实际检测用户环境为 4K（3840x2160）屏幕并启用了 250% 缩放（240 DPI）；
  2. 初始版本硬编码了 96 DPI 下的固定列宽（90px、80px 等）与固定高度（表头 34px、行高 28px、底部面板 72px），并在关闭时将未缩放的绝对像素值持久化至 `%LOCALAPPDATA%\UsageTray\settings.json` 的 `codexHistoryColumnWidths`；
  3. 在 250% DPI 字体放大 2.5 倍后，80px 实际仅能容纳 1~2 个汉字，导致所有表头文字均被省略号截断；
  4. 底部面板原先使用 `TableLayoutPanel` 分配 33% 相对高度，在 72px 总高下无法容纳三行 35px 高度的大字体，导致文字边界相互重叠覆盖。
- **重构与适配**：
  - **DPI 动态缩放引擎**：引入基于 `DeviceDpi` 的动态缩放比例（`scale = DeviceDpi / 96.0f`），基准列宽乘以 2.5 后适配高分屏（例如 4K 下主力模型列宽扩充至 375px，周期区间扩充至 700px）；
  - **字体感知行高与表头高度**：通过 `TextRenderer.MeasureText` 基于当前字体与 DPI 动态推导表头高与每行高度，并在 DPI 变化时动态重绘；
  - **消除子控件重叠**：移除 `TableLayoutPanel`，底部详情面板采用基于行高与边距的确定性像素级垂直布局（`LayoutDetailLabels`），保证 3 行文本（周期与快照、Token 明细、订阅参考与预估）各自具备专属纵向区域，完全杜绝踩踏；
  - **智能防挤压列宽保护（`minSafeWidth`）**：在 `ApplyColumnWidths` 中检测历史保存的配置，若任何列宽低于当前 DPI 安全阈值（`defaultScaled * 0.75`），自动重置为高分屏标准安全宽度，瞬间纠正旧配置遗留的挤扁数据；
  - **用户配置重置**：清理本地旧配置中被压缩的过小数值，使得程序立即以最舒展的排版呈现。
- **验证与交付**：
  - 全量 160 项单元测试全部通过；通过 `DrawToBitmap` 离屏渲染测试生成高质量预览图验证排版间距；
  - 自动执行本地 Release 发布（`dotnet publish -c Release`），并在后台重新拉起最新产物（`publish\UsageTray.exe`，PID 45432）。

### [FEAT] 新增 Codex 历史周额度周期主面板页面及非固定 7 天动态重置算法
- **修改时间**：2026-09-09 01:39:20（Asia/Hong_Kong）。
- **用户需求**：支持制作一个单独的主面板页面，让 Codex 显示过去数次区间的周额度，并特别注意区分重置情况（不一定完整按照时间为 7 天间隔）。
- **历史周期重构算法**：
  - 以 `(model_or_pool_id, reset_at_utc)` 为权威周期边界；
  - 针对同一模型池按重置时间升序链式推算起始时间：当与前一周期重置时间间隔在 0.5 天至 8.0 天内，严格以实际前一周期截止时刻作为本周期起始时刻；
  - 完美识别升降配（Plus $\leftrightarrow$ Pro）、维护重置与手动调整导致的 1.0天、2.5天、3.8天、6.0天等非整7天周期；
  - 精确推算各周期内的额度变化（起始剩余% → 最低剩余%）、已消耗%及采样快照数。
- **Token 与金额内存高效切片**：
  - 在 `UsageRepository` 中抽象 `GetNormalizedCodexEvents` 与 `ConvertAuditsToBuckets`，全生命周期只做一次归一化事件解析；
  - 数十个历史周期的 Token 消耗及订阅参考金额计算均在内存中基于时间窗口过滤与聚合，耗时低于 20ms，界面秒开无卡顿；
  - 满额样本预估传入周期末次快照时刻 `now`，确保历史有效快照不被判定为过期，精准复现历史外推。
- **主面板独立页面交互 (`CodexHistoryControl`)**：
  - 在 `MainForm` 底部 TabControl 增加第 4 个页面「Codex 周历史」；
  - 包含顶部模型池下拉筛选（全部 / Codex 主力模型 / GPT-5.3 Spark / Codex Reserve）及周期计数统计；
  - DataGridView 双缓冲自绘，视觉区分「● 进行中」（高亮深绿粗体）与「已重置」（常规灰字）；
  - 底部详情面板联动显示选中周期的完整精确时间戳、未命中输入/缓存读取/缓存创建/输出 Token 明细分布与预估说明；
  - 支持列宽手动拖拽拉伸并与 `WindowGeometryPersistence` 及 `AppSettings.CodexHistoryColumnWidths` 自动持久化保存。
- **验证与交付**：
  - 新增 `CodexHistoricalCyclesTests`，包含非 7 天重置推算、多池隔离、UI 筛选与离屏渲染 4 项新用例；全量 160 项单元测试 100% 全部通过；
  - 完成本地 Release 构建与发布，最新交付产物已部署至根目录 `publish` 并在后台运行（PID 43876）。

### [FIX] 恢复用户约定的 Luna 计价、Codex 周计算和面板点击关闭
- **修改时间**：2026-09-09 00:47:29（Asia/Hong_Kong）。
- **用户反馈**：Reserve/Auto-Review 应按设置原有说明使用 Luna；Codex 本次周额度计算消失；单击面板后点击无法关闭。
- **计价 v5**：恢复两个模型的 Luna 参考映射，缓存、长上下文、Fast 与 Luna 一致。将 v4 内置未定价规则加入迁移历史，现有配置自动恢复；保留其他自定义价格。设置说明同步恢复为用户约定的参考计量依据。
- **Codex 周计算**：Auto-Review 不再使主力本周金额整体变为未知。外推先隔离同一重置周期，其他周期快照不再清空有效基线。实际数据副本恢复显示本周参考金额 $42.86、满额样本估算约 $356.20（采样时数值，非固定额度）。
- **解析器 v11**：实际日志表明 ProLite Spark 曾使用 `limit_id=codex` + Spark 模型 + 5h/周双窗口。增加该明确格式的兼容，阻止它再进入主力周期；通用 ID 的单周主力窗口判断保留。
- **交互**：补回鼠标松手点击关闭；拖动不误关，拖动后下一次点击可关；鼠标捕获与坐标使用真实子控件；主动关闭同时取消固定状态；Esc 可关。弹窗限于工作区，避免长面板把 Codex 区域放到屏幕之外。
- **验证**：156 项 Release 测试通过，包含两类规则迁移、完整 Luna 档位、周金额/估算显示、跨周期记录、实际 Spark 格式、点击/拖动关闭与多屏边界回归；真实数据离屏面板核对通过。
- **交付**：自动执行 Release 发布并重启根目录 publish 中的修正版；更新前数据备份保留在 `其他/accuracy-audit-20260908/obj/followup-before-runtime`。


### [FIX] 额度准确性与订阅参考金额口径修复
- **变更时间**：2026-09-09 00:04:36（Asia/Hong_Kong）。
- **用户选定口径**：订阅消耗参考价值，保留非促销基准。撤回将 Sol API 促销价差异认定为订阅价格错误的结论；Sol 输入/输出基准保留 $5/$30。界面统一为“订阅参考金额”。
- **额度准确性**：修复普通 5.3 与 Spark 混淆、显式池 ID 优先级、bengalfox 缺少名称，以及实际旧 Reserve 两种上报格式；解析器 v10 自动重建。尊重实际 5h 窗口；过期标为待同步，旧快照单独标注采样时间。
- **周期与金额**：没有有效周期的池不纳入本次周统计；Codex/Antigravity 查询采用半开区间，各池独立。删除按日汇总冒充精确采样区间的回退。周归一化保留服务档位。
- **参考基准 v4**：Codex Astra/5.6/5.5 Fast 采用订阅 2.5 倍，5.4 采用 2 倍，长上下文同步；修正 Opus 4.6 与 Gemini Pro 缓存基准。Gemini 3.6/3.7/3.8 Flash 使用官方列示常规基准。Spark/Reserve/Auto-Review 和未核实别名保留 Token 并标为未定价，合计不伪装为完整金额。价格更新改为随软件发布的基准迁移，保留自定义规则。
- **外推约束**：满额金额采用同周期两次真实快照与对应本机 Token 金额。至少观察下降 5 个百分点、末次快照在 15 分钟内；回升/周期/套餐变化、超过 36 小时采样间隔重新建立基线。缺少或未定价样本暂停输出，统一标明“本机样本外推”。
- **验证**：147 项 Release 回归通过（此前 146 项 Debug 回归通过）；实际数据副本复核当前周输入从 280,916,251 修正为 37,950,922，排除旧 Reserve 242,965,329 输入；离屏额度面板检查通过。移除自动测试中会打开并迁移真实业务数据库/价格文件的调试用例。
- **交付**：主项目发布目录固定为根目录 `publish`；启动路径解析支持旧嵌套发布目录。完成 `dotnet publish -c Release` 后以根目录产物更新本机运行版本。详情见 `其他/accuracy-audit-20260908/修复复核.md`。

## 2026-09-08

### [FIX/UI & ARCH] 统揽托盘悬浮、点击面板与主面板三处展示架构，建立 Single Source of Truth 单源事实机制，修复 Tooltip 误读 Spark 77% 并重构 QuotaPopupForm
- **变更时间**: 2026-09-08 21:55
- **需求背景与业务决策**:
  - 用户反馈托盘 Tooltip 悬浮提示出现错误（将 Spark 的 77% 错误冠以 `Codex 77%` 替代了主力模型的真实 98% 额度）；
  - 用户明确指示系统共有 3 处展示出口：
    1) 悬浮显示（托盘 Tooltip）；
    2) 点击面板（单击托盘弹出的悬浮窗 `QuotaPopupForm`）；
    3) 双击主面板（双击打开的主窗口 `MainForm`，包括额度 Tab 与顶部卡片）；
    必须**确保它们 100% 引用同一个源头（Single Source of Truth: `DashboardSnapshot`），消除各自提取、过滤或重绘导致的显示错乱与分化**。
- **技术实现与重构清单**:
  1. **Tooltip 逻辑解耦与防混淆修复 (`QuotaDisplayFormatter.cs`)**:
     - 根因分析：先前 `BuildCompactText` 使用 `OrderBy(RemainingFraction)` 选取 Codex 周额度，当 Spark 周额度（77%）低于主力通用（98%）时，错误拾取了 Spark 额度并冠以 `Codex 77%`；
     - 彻底解耦主力通用模型与 Spark 模型：分别提取 `stdWeekly`、`sparkWeekly`、`codexReserve` 与 `std5h`/`spark5h`；
     - Pro 及以上套餐主力通用不设 5h 额度，Tooltip 标准格式化为：`AG 5h X% / 周 Y% | Codex 主力% / Spark %`（如触发备用额度则追加 ` / 备 Z%`），严格保证文本长度在 63 字符安全限制内，彻底消除误导。
  2. **点击面板架构重构 (`QuotaPopupForm.cs`)**:
     - 彻底删除原先 600+ 行重复维护的 `OnPaint`、`MeasureHeight` 与格式化文本生成逻辑；
     - 重构为直接宿主承载 `QuotaSummaryControl` 控件（`_contentControl = new QuotaSummaryControl { Dock = DockStyle.Fill, ShowHeader = true, ShowDismissHint = true, ShowFooterNote = false }`）；
     - 实现点击面板与双击主面板额度 Tab 共享同一套渲染逻辑与样式规范，消除两处 UI 的代码分化，两处像素级完全一致。
  3. **双击主面板顶部卡片与重置周期增强 (`MainForm.cs`)**:
     - 顶部卡片 Codex 金额支持多通道：存在 Spark/Reserve 时清晰呈现 `Codex: $主力 | $Spark`（悬浮提示展示各自明细）；
     - 额度重置周期提示在周窗口模式下自适应呈现 `主力: ... | Spark: ...` 多通道倒计时。
  4. **单元测试与回归保障 (`EnhancementTests.cs`)**:
     - 新增 `CompactTooltip_SingleSourceOfTruth_DecouplesStandardAndSparkWithoutMisleading77Percent` 测试用例，严格验证 Tooltip 包含 `Codex 98% / Spark 77%` 且不混淆；
     - 全套 128 项单元测试 100% 全部通过。

- **需求背景与业务决策**:
  - 用户指出：先前版本为清理跨周陈旧 Reserve，武断增加了 `if (isNonPlus) return true;`（将非 Plus 会员的所有 Reserve 全部强行排除），导致 PRO 会员即使触发 Reserve 也无法显示；
  - 明确真正业务规则：**若 Reserve 当前没有被触发（即在当次刷新/当前活跃状态中未检测到有效 Reserve），就不显示（PRO 会员显示主力通用与 Spark 两行）；若当前检测到 Reserve 被触发，则必须持续并列显示，此时 PRO 会员应并列展示三行内容（主力通用、Spark、Reserve）**。
- **技术实现与修复清单**:
  1. **移除套餐静态限制与引入动态活跃检测 (`UsageRepository.cs`)**:
     - 彻底删除 `isNonPlus` 硬编码拦截；
     - 增加 Reserve 动态活跃触发三层校验规则：
       1) **有效期校验**：若 Reserve 已过重置时间（`ResetAt <= UtcNow`），说明已失效重置，自动排除并物理清理；
       2) **周期有效性**：若 Reserve 采集时间早于当前主力模型的周周期起始时间，说明属于过去历史周期的残留，当前周期未触发，自动排除；
       3) **活跃触发判定**：若主力通用模型额度充沛（剩余 > 5%），且 Reserve 产生时间明显早于最新主力快照（非同批活跃交互），说明当前未处于 Reserve 触发状态，自动排除；反之（主力额度耗尽、或 Reserve 是最新产生的有效快照、或同批产生），判定处于活跃触发状态，完整保留。
  2. **聚合模型动态周期支持 (`UsageAggregator.cs`)**:
     - 同步移除 `BuildCodexWeeklyCycles` 中针对非 Plus 套餐将 `reserveQuota` 设为 null 的硬编码；
     - 对齐三层活跃触发校验规则，若当前处于触发状态，保留为有效 `reserveCycle`，并存入 `CodexWeeklyCycles` 列表；
     - 周窗口与周订阅统计自适应支持三通道并列计算：主力模型、GPT-5.3 Spark 与 Codex Reserve。
  3. **单元测试覆盖与验证 (`CodexReserveQuotaTests.cs`)**:
     - 新增 `GetLatestQuotas_IncludesReserve_WhenProLiteUserTriggersReserve` 测试，验证 ProLite 用户触发 Reserve 时返回 3 个配额快照，生成 3 个周周期并在弹窗文本中完整展示 3 行周窗口与 3 行周消耗；
     - 全套 127 项单元测试 100% 全部通过。

### [FEAT/REFACTOR] 实现 Codex 双通道额度完全解耦（主力通用模型 vs GPT-5.3 Spark 独立并列展示与周满额推算），Pro+ 套餐隐藏主力通用 5 小时限额
- **变更时间**: 2026-09-08 21:30
- **需求背景与业务决策**:
  - 用户明确指示：不要把 GPT-5.3 Spark 的用量当成全局用量，将其直接做成一个**独立的额度显示**，对齐 Antigravity（Gemini 与 Claude/GPT 并列独立）的双通道架构；
  - Pro 及以上高阶套餐主力通用模型原生没有 5 小时限额，不要显示主力通用模型的 5 小时限额字样，但必须独立展示 GPT-5.3 Spark 的 5 小时额度及周额度。
- **技术实现与重构清单**:
  1. **解析器多池解耦 (`CodexJsonlParser.cs`)**:
     - `ParserVersion` 升级至 `9`；
     - `ExtractRateLimits` 结合 `currentModel`、`limit_id` 与 `limit_name` 精确判定 Spark 模型（匹配 `spark`/`5.3`/`bengalfox`），分别输出 `codex-spark-5h`（DisplayLabel: `GPT-5.3 Spark (5小时额度)`）与 `codex-spark-weekly`（DisplayLabel: `GPT-5.3 Spark (周额度)`）；
     - 非 Spark 模型输出 `codex-5h` 与 `codex-weekly`（DisplayLabel: `Codex 主力模型 (周额度)`）；
     - `ClassifyWindow` 针对 Spark 模型将 `primary` 明确归为 `5h`，`secondary` 明确归为 `weekly`。
  2. **仓储层三池独立查询与 Token 独立过滤 (`UsageRepository.cs`)**:
     - `GetLatestQuotas` 采用三池独立 `MAX(captured_at_utc)` 算法：标准通用池、Spark 专用池、Reserve 备用池三通道独立取最新，绝不相互冲刷覆盖；
     - `GetCodexUsageInPoolWindows` 新增 `sparkStartUtc` 与 `sparkEndUtc` 参数，使用 `UsageAggregator.IsCodexSparkModel` 精确过滤 Spark 池 Token 消耗，确保两池 Token 与金额完全物理隔离。
  3. **聚合调度与双周周期建模 (`UsageAggregator.cs`, `UsageViews.cs`)**:
     - `DashboardSnapshot` 新增 `CodexWeeklyCycles` 列表，存储 `(Standard, Spark, Reserve)` 多通道周周期；
     - `BuildSingleCodexCycle` 严格按池过滤 tokens，计算各自的本轮周消耗及满额预估；
     - 当选择“本次周额度”区间时，自动提取主力通用周窗口作为主统计时间范围。
  4. **UI 并列独立渲染与自适应排版 (`QuotaSummaryControl.cs`, `QuotaPopupForm.cs`, `QuotaDisplayFormatter.cs`)**:
     - 5 小时窗口：Pro 及以上套餐仅保留并展示 Spark 5h 窗口（如 48% 剩余），隐藏主力通用 5h，自适应排版高度并保留倒计时；
     - 周窗口：并列独立展示主力模型周额度与 GPT-5.3 Spark 周额度；
     - 周订阅统计与预估：分别展示 `Codex 主力模型 本轮周消耗: $... (已消耗 ...)`、`Codex 主力模型 周满额预估: 约 $...` 与 `GPT-5.3 Spark 本轮周消耗: $... (已消耗 ...)`、`GPT-5.3 Spark 周满额预估: 约 $...`；
     - 简化 `ShortLabel`，移除冗余的 `(5小时额度)` / `(周额度)` 后缀，显示美观清晰。
  5. **自动化测试与全量数据库重建**:
     - 新增 `BuildSnapshot_DecouplesStandardAndSparkWeeklyCycles` 单元测试，测试套件 126 项测试全部 PASS；
     - 全量重新扫描用户 447 个会话文件，成功生成标准主力周额度（98% 剩余，本轮周消耗 $6.63，周满额预估 $331.59）与 GPT-5.3 Spark 双额度（5h 48% 剩余，周额度 77% 剩余，本轮周消耗 $0.32，周满额预估 $1.37）。

### [FIX/CODEX & UI] 修复 GPT-5.3-Codex-Spark 独立配额冲刷通用主力周额度致显示消耗 1% 与预估异常，Pro+ 套餐隐藏 5 小时限额
- **变更时间**: 2026-09-08 17:15
- **缺陷现象与深度排查**:
  - 用户反馈 Codex 配额摘要错误显示为“已消耗 1%”（实际主力模型已消耗近 90%），且推算出的周满额价值异常膨胀至 $29,521.52；
  - 经追踪 JSONL 与数据库排查根因：
    1. **Spark 辅助模型自带独立配额池**: 用户会话中调用了 `GPT-5.3-Codex-Spark`（内部代号 `codex_bengalfox`），其返回了独立的 5 小时与 7 天（已用 1%）rate limits，产生时间比主力通用模型晚 11 秒；
    2. **旧版解析器与仓储未隔离 Spark 池**: `CodexJsonlParser` 先前将所有非 Reserve 额度统一打上 `codex-primary` 与 `codex-weekly`，且 `UsageRepository.GetLatestQuotas` 仅按 `window_kind` 聚合取最新，导致 Spark 的 1% 覆盖了主力模型的 90%；同时周周期聚合使用了 Spark 的 1%（`$295.21 / 0.01 = $29,521`）导致周满额异常失真；
    3. **Pro+ 套餐显示不适用的 5 小时限额**: Pro / ProLite 等高阶套餐原生不设 5 小时限额，此前界面显示的 5h 100% 实为 Spark 辅助模型的 primary 窗口误显。
- **技术实现与修复清单**:
  1. **Spark 配额池独立归类 (`CodexJsonlParser.cs`)**:
     - `ParserVersion` 升级为 `8`（触发全量扫描清理历史脏数据）；
     - 识别 `limit_id` 或 `limit_name` 包含 `spark`/`bengalfox` 的快照，归类为独立 `codex-spark-*` 池，与标准通用额度彻底隔离；
  2. **数据仓储复合唯一键查询 (`UsageRepository.cs`)**:
     - 改进 `GetLatestQuotas` 查询逻辑，分别获取标准池、Reserve 池与 Spark 池最新快照；
     - 分组键从单 `WindowKind` 升级为 `${item.ModelOrPoolId}_{item.WindowKind}` 复合键，杜绝不同池之间时间戳相互冲刷覆盖；
  3. **聚合引擎隔离 Spark 快照 (`UsageAggregator.cs`)**:
     - 新增 `IsSparkSnapshot` 与 `IsProOrAbovePlan` 识别；
     - 周周期与满额推算显式过滤 Spark 快照，确保周满额价值基于主力通用模型真实配额推算；
  4. **Pro 及以上套餐隐藏 5 小时限额展示 (`QuotaSummaryControl.cs`, `QuotaPopupForm.cs`, `QuotaDisplayFormatter.cs`)**:
     - 当检测到 ProLite / Pro / Team / Enterprise 套餐时，在额度 Tab、悬浮弹窗及托盘气泡提示中隐藏 5 小时窗口，高度动态自适应收缩；
     - 完整保留各有效窗口的“已重置”倒计时状态及底部过期提示；
  5. **自动化测试覆盖与验证**:
     - 新增 `CodexReserveQuotaTests`（Spark 配额解析与复合键独立获取测试）及 `EnhancementTests`（Pro 套餐隐藏 5 小时限额自适应高度与格式化测试）；
     - 全套 125 项测试全部通过（0 失败）。

## 2026-09-07

### [FIX/DEPLOY & DATA] 修复开机自启加载旧版本路径导致界面复发 Plus 徽章缺陷，规范化自启目标并彻底清理陈旧 Reserve 数据
- **变更时间**: 2026-09-07 15:05
- **缺陷现象与深度排查**:
  - 计算机重启后，用户反馈悬浮窗与图标再次变为 `Plus`，并再次出现 8 月 30 日已重置过期的 `Codex Reserve` 卡片；
  - 经排查定位真正根因：
    1. **开机自启注册表指向旧版构建目录**: 注册表 `HKCU:\Software\Microsoft\Windows\CurrentVersion\Run\UsageTray` 记录的自启动路径为 `src\UsageTray\bin\Release\net8.0-windows\publish\UsageTray.exe`，该文件停留在 2026-09-06 15:40（昨日未修复版本）；而昨日修复的新版本仅输出到了根目录交付路径 `publish/UsageTray.exe`。开机时 Windows 自动启动了未修复的旧版 exe；
    2. **数据库中存留历史过期 Reserve 快照**: 数据库中保留有 206 条早于 9 月 5 日的过期 `codex-reserve` 快照，旧版 exe 一旦运行即重新将其作为最新数据读取并渲染。
- **技术实现与修复清单**:
  1. **自启路径规范化与自动纠偏 (`StartupManager.cs`, `TrayApplicationContext.cs`)**:
     - `StartupManager.GetCanonicalExecutablePath` 优先锁定工程根目录标准交付路径 `publish\UsageTray.exe`；
     - 增加 `SyncStartupPathIfEnabled`，在托盘初始化时自动检测并修正注册表自启动路径，杜绝指向开发或旧构建目录；
  2. **数据层彻底物理清理过期 Reserve (`UsageRepository.cs`)**:
     - 新增 `CleanupStaleReserveSnapshots`，自动物理删除重置时间已过且超过 24 小时的历史孤儿 Reserve 快照；并在 `GetLatestQuotas` 开始时主动触发清理，直接清空数据库中的历史陈旧快照；
     - 增强 `GetLatestQuotas`：当最新标准快照缺失 `PlanTier` 时回溯查询最近已知计划，三道防线彻底绝缘陈旧 Reserve；
  3. **统一双目录发布交付机制**:
     - 每次发布同时生成并同步根目录 `publish\` 与 `src\UsageTray\bin\Release\net8.0-windows\publish\`，确保两个目录的 exe 100% 同步更新；
  4. **注册表校正与服务热启**:
     - 将注册表开机启动项立即修正为标准交付产物路径；
     - 重启常驻托盘服务（PID: 16708），全套 113 项测试 100% 通过。

## 2026-09-06

### [FIX/UI & DATA] 修复升级 Pro/ProLite 会员后悬浮窗仍错误显示 Plus 徽章及历史已过期 Reserve 额度周期残留显示的缺陷
- **变更时间**: 2026-09-06 16:15
- **缺陷现象与根因排查**:
  - 用户反馈截图显示：即便升级为 Pro 会员，悬浮窗标题徽章依然显示为 `Plus`，且周额度窗口中仍然显示已过期数天的 `Codex Reserve: 6% 剩余 (重置 09-03 19:17, 已重置)`，同时在周订阅统计中继续残留显示 `Codex Reserve 本轮周消耗: $6.43`；
  - 经深入排查定位两个根因：
    1. **PlanTier 提取未排序且未优先选取非 Reserve 配额**: 在 `QuotaPopupForm.cs`、`QuotaSummaryControl.cs` 及 `QuotaDisplayFormatter.cs` 中，均使用 `snapshots.Select(s => s.PlanTier).FirstOrDefault()`；由于数据库返回结果中 `codex-reserve` 按字典序排在 `codex-weekly` 前面，导致升级前 8 月 30 日最后一次残留的 Plus 快照被优先命中，覆盖了今天最新的 `ProLite` 配额；
    2. **GetLatestQuotas 跨历史全局检索孤儿 Reserve 快照**: `UsageRepository.GetLatestQuotas` 使用 `(q.model_or_pool_id = 'codex-reserve' AND q.captured_at_utc=(SELECT MAX(...) ...))` 独立检索 Reserve，当用户升级到 Pro/ProLite 或进入无 Reserve 的新周期时，历史早已过期的 Reserve 快照由于没有新 Reserve 写入而被当作“最新配额”永久返回并渲染在界面上。
- **技术实现与修改清单**:
  1. **数据持久层剔除失效/陈旧 Reserve 快照 (`UsageRepository.cs`)**:
     - 在 `GetLatestQuotas(ProviderKind.Codex)` 中，检测用户最新标准额度的 `PlanTier`；若为 `ProLite`、`Pro`、`Team` 等非 Plus 计划（本身无 Reserve 配额），或 Reserve 快照早于最新主快照超过 24 小时且其重置时间已过，自动在结果集中过滤剔除陈旧 Reserve；同时调整排序，确保标准额度优先于 Reserve 额度返回；
  2. **聚合引擎周期过滤 (`UsageAggregator.cs`)**:
     - 在 `BuildCodexWeeklyCycles` 与周周期窗口计算中，同步剔除针对非 Plus 会员或早于当前主周期的过期 Reserve 快照，杜绝残留孤儿 Reserve 周期被构建并显示；
  3. **UI 徽章与标题 PlanTier 提取加固 (`QuotaPopupForm.cs`, `QuotaSummaryControl.cs`, `QuotaDisplayFormatter.cs`)**:
     - 统一提取逻辑：按 `CapturedAt DESC` 倒序，优先提取非 Reserve 的最新标准快照 `PlanTier`，彻底避免字典序或陈旧快照抢占；
  4. **回归测试覆盖 (`CodexReserveQuotaTests.cs`)**:
     - 新增 `GetLatestQuotas_FiltersOutReserve_WhenUserUpgradedToProLite` 测试，验证升级至 ProLite 后历史 Plus Reserve 被自动过滤；
     - 新增 `GetLatestQuotas_FiltersOutReserve_WhenReserveSnapshotIsExpiredAndStale` 测试，验证过期陈旧的 Reserve 快照被正确排除；
     - 全套 113 项测试 100% 通过；
  5. **交付物发布与进程热启**:
     - 自动执行 `dotnet publish -c Release` 并在 `publish/` 交付产物目录更新主程序；通过 WMI 安全重启常驻托盘服务。

### [FIX/CODEX & FEAT/PRICING] 修复 Pro/ProLite 会员识别为 Plus/Reserve 缺陷、compacted 事件导致 Review/Astra 破折号熔断，以及补充 gpt-5.3-codex-spark 模型定价
- **变更时间**: 2026-09-06 15:40
- **缺陷现象与排查分析**:
  1. **软件仍错误显示 Plus，ProLite 额度被当成 Reserve**:
     - 用户升级为 Pro / ProLite 会员后，Codex CLI 输出的 `rate_limits` 结构变为 `plan_type: "prolite"`，`primary: { used_percent: 9.0, window_minutes: 10080 }`，`secondary: null`（官方 Pro/ProLite 不设 5 小时限制，仅有单主周额度）；
     - `CodexJsonlParser.cs` 先前逻辑错误假设“无 secondary 且 primary >= 10000 即为 Reserve”，将 ProLite 主周额度误打上 `codex-reserve`；
     - 导致数据库查询最新非 Reserve 额度时，只能拿到升级前 Plus 的历史快照（`plan: plus`），UI 死锁显示 Plus，且 ProLite 额度被当成 Reserve 额度展示。
  2. **Review 与 Astra 模型 API 等值金额显示破折号 `—`**:
     - Codex CLI 在进行上下文压缩时输出 `type: "compacted"` 事件，其 `replacement_history` 嵌套包含历史消息及 `input_tokens`；
     - `CodexJsonlParser.cs` 的 fallback 循环误将其匹配为 `EventType = "usage", LastUsage = null` 快照，导致 normalizer 将其判定为 `CodexRequestUsageQuality.Unknown`，累加 `RequestShapeUncertainCount > 0`；
     - `PricingService.Calculate()` 触发 `HasRequestShapeUncertainty` 保守熔断，返回 `CostQuality.RequestShapeUnavailable` 且金额为 null（前端破折号）；同时影响了 `codex-auto-review` 及 `gpt-6-astra` 部分会话。
  3. **新用模型未统计成功 (`gpt-5.3-codex-spark`)**:
     - 用户会话中调用了 OpenAI 新轻量代码模型 `gpt-5.3-codex-spark`，由于规则库中未配置该模型，导致计价提示“未配置模型价格”，金额为空。
- **技术实现与修改清单**:
  1. **重构 Reserve 判定与规范化 PlanTier (`CodexJsonlParser.cs`)**:
     - 在 `ExtractRateLimits` 中重构 Reserve 判定：仅当模型名明确包含 `reserve`（如 `gpt-reserve`），或者在明确为 `plus` 降级订阅下无 secondary 且 primary >= 10000 时，才判定为 Reserve；
     - 当计划为 `pro`、`prolite`、`team`、`enterprise` 或使用标准模型时，单个周窗口准确判定为主订阅周额度（`codex-weekly`）；
     - 动态从日志中更新 `planTier`（移除 `??=` 锁死逻辑），并新增 `NormalizePlanTier` 统一规范化显示名称（如 `prolite` $\to$ `ProLite`，`plus` $\to$ `Plus`）；
  2. **过滤上下文压缩非权威事件 (`CodexJsonlParser.cs`)**:
     - 在 `TryReadTokenEvent` 入口处显式拦截并丢弃 `compacted`、`context_compacted` 与 `token_usage_record`，彻底杜绝上下文压缩历史被解析为伪用量快照；
  3. **解析器版本升级至 7 (`CodexJsonlParser.cs`)**:
     - 将 `ParserVersion` 从 6 升级至 7，自动触发本地 SQLite 数据库历史脏数据清理与全量重新计算；
  4. **补充 `gpt-5.3-codex-spark` 系列模型定价 (`default-pricing.json`, `PricingService.cs`, `PricingUpdateService.cs`)**:
     - 增加 `gpt-5.3-codex-spark*` 与 `gpt-5.3*` 定价规则（按官方轻量代码模型标准：标准输入 \$0.2/M、缓存读 \$0.02/M、缓存写 \$0.25/M、输出 \$1.2/M，>272K Tokens 长上下文加价）；并在 `PricingUpdateService` 中注册页面抓取映射；
  5. **单元测试与全量真实数据验证 (`CodexRateLimitTests.cs`, `CodexJsonlParserTests.cs`, `PricingServiceTests.cs`)**:
     - 新增 `ParsesProLiteRateLimits_AsStandardWeeklyQuota_NotReserve` 测试，验证 ProLite 单周额度解析为 `codex-weekly`，PlanTier 为 `ProLite`，非 Reserve；
     - 新增 `CompactedEventIgnoredAndDoesNotCauseRequestShapeUncertainty` 测试，验证压缩事件被忽略且用量桶不确定度为 0，金额计算正常；
     - 新增 `Gpt53CodexSparkPricing_CalculatesCorrectCost` 测试，验证 Spark 模型计价准确无误；
     - 运行全量 111 项测试 100% 通过；
  6. **本地数据库全量重建与交付物发布**:
     - 运行 `--rebuild-codex` 全量重构本地数据库，验证今日所有模型（`codex-auto-review`、`gpt-6-astra`、`gpt-5.3-codex-spark`、`gpt-5.6-luna`）不确定度全部归零，API 等值金额全部确切输出（无破折号）；最新配额正确显示 `ProLite`，剩余 91.0%，无旧 Plus 快照残留；
     - 执行 `dotnet publish -c Release` 及输出至 `publish/` 交付目录，并重启托盘常驻进程。

## 2026-09-05

### [FIX/CODEX] 修复新版 Codex CLI (v0.153.3+) token_usage_record 导致 gpt-6-astra 与 codex-auto-review 等值金额显示破折号缺陷
- **变更时间**: 2026-09-05 15:15
- **缺陷现象与排查分析**:
  - 用户反馈截图显示主界面中 `gpt-6-astra`（6.28M 输入 / 151.97M 缓存 / 607.05K 输出）与 `codex-auto-review`（2.82M 输入 / 42.38M 缓存 / 119.99K 输出）的 API 等值金额均显示为破折号（`—`），无法正确显示计费金额；
  - 经深入排查本地真实会话与数据库状态，查明根因：新版 Codex CLI（v0.153.3+）在每个 Turn 会同时输出两行事件：
    1. `{"type": "token_usage_record", "payload": { "usage": {...}, "thread_token_usage": {...} }}`（CLI 内部单次请求跟踪记录）；
    2. `{"type": "event_msg", "payload": { "type": "token_count", "info": { "total_token_usage": {...}, "last_token_usage": {...} } }}`（权威会话全局累计与最后单次用量）；
  - 原解析器在 `CodexJsonlParser.cs` 中未显式拦截 `token_usage_record`，导致其命中旧日志单次用量 fallback。该 fallback 将单次增量误作为累计总额，且赋值 `LastUsage = null`；
  - 随后在 `CodexUsageNormalizer.cs` 中，`LastUsage == null` 的快照被断定为 `CodexRequestUsageQuality.Unknown`，累加 `RequestShapeUncertainCount`，生成带有 `HasRequestShapeUncertainty == true` 的用量桶；
  - 在计价计算时，`PricingService.Calculate()` 检测到 `bucket.HasRequestShapeUncertainty`，直接保守熔断返回 `CostQuality.RequestShapeUnavailable` 且金额为 `null`，前端从而显示为破折号 `—`。
- **技术实现与修改清单**:
  1. **过滤调试事件 (`CodexJsonlParser.cs`)**:
     - 在 `TryReadTokenEvent` 入口处深度遍历并识别 `type == "token_usage_record"`，直接跳过并丢弃，彻底杜绝调试事件进入快照集合；
     - 仅保留并绑定权威 `token_count` 事件（同时包含全局 `total_token_usage` 与精确的单次 `last_token_usage`）；
  2. **解析器版本升级与缓存重构 (`CodexJsonlParser.cs` & `CodexProvider.cs`)**:
     - 将 `CodexJsonlParser.ParserVersion` 从 5 递增至 6，触发版本不一致自动全量重构机制，刷新历史快照与用量数据；
  3. **单元测试与真实样本验证 (`CodexJsonlParserTests.cs` & `PricingServiceTests.cs`)**:
     - 新增 `TokenUsageRecordIgnoredAndDoesNotCauseRequestShapeUncertainty` 测试，模拟新版 Codex 双写场景，断言仅提取 `token_count`、用量桶不确定次数为 0，且计价计算成功输出精确金额（非空）；
     - 读取本地真实 2026-09-05 会话数据实测：`codex-auto-review` 不确定度为 0 且金额精确计算（~\$0.08），`gpt-6-astra` 不确定度为 0 且金额精确计算（~\$10.00，质量为 `ExactTokenSplit`）；
     - 全量 108 项测试 100% 通过；
  4. **全量数据库重构与交付物发布**:
     - 运行 `--rebuild-codex` 重建本地数据库，使历史残存脏数据全量校准；
     - 自动执行 `dotnet publish -c Release` 更新交付产物目录。

### [FEAT/PRICING] 支持 OpenAI GPT-6 (GPT-6 Astra) 官方全套定价体系、长上下文加价及 Fast 模式
- **变更时间**: 2026-09-05 14:30
- **变更背景与实施决策**:
  - OpenAI 于 2026 年 9 月 3 日发布下一代旗舰 Agent 智能体模型 **GPT-6 Astra**（API 主要标识符为 `gpt-6-astra` 与 `gpt-6`），提供 1M 上下文窗口及深度的计算机操作/软件工程能力；
  - 软件全面接入其官方计费标准：
    1. **标准单价**: 输入 \$10.00 / 缓存读取 \$1.00（10%） / 缓存写入 \$12.50（1.25x） / 输出 \$50.00（每 1M Tokens）；
    2. **长上下文加价阶梯**: 单次请求超过 272K Tokens 阈值时，输入/缓存加价 2x（输入 \$20.00、缓存读取 \$2.00、缓存写入 \$25.00），输出加价 1.5x（输出 \$75.00）；
    3. **Fast 模式**: 速率翻倍，费率为 2x 标准费率（输入 \$20.00、缓存读 \$2.00、缓存写 \$25.00、输出 \$100.00；长上下文输入 \$40.00、输出 \$150.00），支持通过 ServiceTier "fast" 或模型通配匹配；
  - 同步解决本地真实 Antigravity 数据库中因使用新模型 `gemini-3.8-flash` 导致的未定价报错，补充 Gemini 3.8 Flash 规则与显示名称映射。
- **技术实现与修改清单**:
  1. **价格文档与模板更新 (`default-pricing.json` & `publish/Pricing/default-pricing.json`)**:
     - 增加 `gpt-6` (Exact)、`gpt-6-astra*` (Wildcard)、`gpt-6-astra-fast*`、`gpt-6-fast*`、`gpt-6*` (Wildcard) 规则；
     - 增加 Antigravity 下的 `gemini-3.8-flash*`（\$0.75 / \$0.075 / \$3.75）及 `gpt-6*` 规则；
     - 升级定价验证日期至 `2026-09-05`。
  2. **计价引擎与内置规则 (`PricingService.cs`)**:
     - `AddLongContextPrice()` 中增加 GPT-6 与 GPT-6 Fast 的长上下文阶梯定价逻辑；
     - `BuiltInDefaults()` 中内置完整的 GPT-6 系列及 Gemini 3.8 Flash 价格规则，支持 ServiceTier `fast`。
  3. **热更新与官方页面映射 (`PricingUpdateService.cs`)**:
     - 在 `OpenAiModelPages` 中注册 `gpt-6`、`gpt-6*`、`gpt-6-astra*` 的官方文档抓取地址；
     - 在 `GeminiModels` 中加入 `gemini-3.8-flash`。
  4. **Antigravity 历史解析模型推断 (`AntigravitySqliteHistoryParser.cs`)**:
     - `InferModelFromDisplayName` 中新增 `gpt-6` / `astra` 以及 `gemini-3.8-flash` 关键字的自动解析映射。
  5. **单元测试与真实数据不变性验证 (`PricingServiceTests.cs`)**:
     - 新增 `Gpt6AstraCalculatesCorrectStandardAndLongContextCost` 测试用例，全面验证标准单价、带缓存写入单价、>272K 长上下文阶梯单价以及 Fast 模式 / ServiceTier 计费结果；
     - 补充 `DisplayNameInferenceResolvesUnknownModels` 与 `AutoMergeMissingRulesOnLoadOrCreate` 断言；
     - 全套 106 项单元测试与包含 237 个本地数据库、34,898 个 generation 的真实数据不变性测试 100% 通过（0 失败，0 未计价）。
  6. **文档同步 (`项目文档.md`, `CODE_INDEX.md`)**:
     - 同步更新系统架构与计价口径说明。

---

## 2026-08-31

### [FEAT/PRICING] 落地 Codex `codex-auto-review` 审查模型定价规则并在设置界面补充不确定性说明（方案 B）
- **变更时间**: 2026-08-31 03:52
- **变更背景与实施决策**:
  - 用户确认采用方案 B，将 `codex-auto-review` 映射为 `GPT-5.6 Luna` 轻量级价格体系（输入 \$0.2 / 缓存 \$0.02 / 写入 \$0.25 / 输出 \$1.2，>272K 加价），以便在产生大量审查 Token 时提供合理的 API 等值估算；
  - 同时在设置窗口中向用户明确说明 Auto-Review 模型与配额扣减的不确定性及基于官方文档与历史重置窗口逆推的判定依据。
- **技术实现与修改清单**:
  1. **内置规则与模板更新 (`PricingService.cs` & `default-pricing.json`)**:
     - 在 `PricingService.BuiltInDefaults()` 与 `AddLongContextPrice()` 中增加 `codex-auto-review*` 匹配规则；
     - 同步更新 `src/UsageTray/Pricing/default-pricing.json` 与 `publish/Pricing/default-pricing.json`；
  2. **设置界面说明增强 (`SettingsForm.cs`)**:
     - 在“当前模型与定价规则”区域增加专门的「Codex Auto-Review 审查模型计量说明」板块，详细阐述审查调用的机制、额度折算的不确定性及当前采用 Luna 价格体系逆推的技术依据；
  3. **单元测试覆盖 (`PricingServiceTests.cs`)**:
     - 增加 `CodexAutoReviewModelCalculatesCostAtLunaPricing` 测试用例，验证 1100 万 Token（含 1000 万缓存）的标准计算结果（\$0.52），全套 105 项单元测试 100% 通过。

### [ANALYSIS/QUOTA] 完成 Codex `codex-auto-review` 实际额度权重与计价假设全量历史逆推分析
- **变更时间**: 2026-08-31 03:40
- **研究背景与目标**:
  - 利用本地 333 个历史 Codex 会话 JSONL、56,552 条原始快照以及独立重置窗口切割机制，定量逆推 `codex-auto-review` 实际按照什么模型/权重消耗用户的 5h 与周配额；
  - 核心对比两大假设：假设 A（`GPT-5.6 Luna`）vs 假设 B（`GPT-5.4` 旗舰）；
- **核心结论与数据发现**:
  1. **数量级排除 GPT-5.4 旗舰定价**: 在 8 月 30 日 1.18 亿 Cache Read 的纯净 Reserve 隔离案例中，若按 GPT-5.4 计价，理论消耗高达 160.2% 的 5h 额度与 26.7% 的周额度，严重背离实际观测（34% 5h 与 5%~6% 周额度），过估达 4.7x~5.3x，在物理与统计上被完全排除；
  2. **轻量级模型高度吻合**: 若按 `GPT-5.6 Luna` 计量，理论消耗为 12.8% 的 5h 额度与 2.14% 的周额度，处于完全一致的数量级；其隐含配额倍率约为 Luna 标准价的 $2.3\times \sim 2.7\times$（或约为 `GPT-5.4 Mini` 标准价的 $0.70\times \sim 0.85\times$）；
  3. **产出文档与工具**: 输出完整逆推分析报告 [`codex_auto_review_reverse_engineering.md`](codex_auto_review_reverse_engineering.md)，并在 `CodexAudit` 工具中固化了无泄漏窗口切割与残差分析诊断链路。


### [FIX/QUOTA] 修复 Codex 本次周额度查询窗口未区分标准池与 Reserve 池导致主面板与周订阅面板金额不一致的缺陷
- **变更时间**: 2026-08-31 01:50
- **变更背景与根因定位**:
  - 用户反馈主界面左上角卡片与表格中的 Codex 本周金额（如 \$41.55）与右侧额度面板中的“Codex 标准额度 本轮周消耗”（如 \$2.12）出现严重不一致；
  - 经排查定位根因：在选择“本次周额度”时，`UsageAggregator` 将标准池周期（如 08-30~09-06）与 Reserve 备用池周期（如 08-27~09-03）简单合并为一个 10 天的并集大窗口（08-27~09-06），并将此窗口直接用于查询所有 Codex 模型的 Token；导致上周期的历史标准模型消耗（08-27 至 08-30 的 \$39.43）错误泄漏并累加到当前周期的标准额度中；而右侧额度面板按精准的独立周周期（08-30~09-06）统计，因此两处显示不一致；
- **技术实现与优化**:
  1. **分池独立周期过滤 (`UsageRepository.GetCodexUsageInPoolWindows`)**:
     - 在仓储层新增 `GetCodexUsageInPoolWindows`，对标准模型（`!IsCodexReserveModel`）和 Reserve 模型（`IsCodexReserveModel`）分别按各自专属的周重置窗口（`standardStart~standardEnd` 与 `reserveStart~reserveEnd`）进行精准过滤聚合；
  2. **聚合引擎联动 (`UsageAggregator.cs`)**:
     - 当 Provider 为 Codex 或全部时，调用 `GetCodexUsageInPoolWindows` 聚合数据，确保主面板卡片、图表、按模型/按项目表格与右侧额度面板的统计口径 100% 严密对齐；
  3. **单元测试与发布**:
     - 增加 `WeeklyCycle_ProperlySegregatesStandardAndReserveWindows_PreventingOldUsageLeak` 单元测试，全套 104 项测试 100% 通过；已执行 Release 自动交付发布。

### [FIX/UI] 统一 Prefill 速率命名规范并支持同时展示「未命中」与「命中」速率
- **变更时间**: 2026-08-31 00:26
- **变更背景与问题定位**:
  - 用户反馈各模型/Session 的预估速率展示不一致（部分行仅显示 `Prefill(未命中)`，部分行仅显示 `Prefill(缓存)`），且命名用词不统一；
  - 经排查定位根因：`TokenSpeedEstimate.ToShortDisplayString()` 中错误使用了 `else if` 互斥分支，导致在同时存在冷启动与命中样本时吞掉了缓存读取速率，而在仅有缓存读取样本时则显示了 `Prefill(缓存)`；
- **技术实现与优化**:
  1. **格式与命名统一 (`TokenSpeedEstimate.cs`)**:
     - 移除 `else if` 互斥，支持同时完整呈现 **未命中速率** 与 **命中速率**；
     - 统一术语为 `未命中`（Prompt 首次矩阵计算）与 `命中`（KV Cache 检索与加载），如 `未命中 ~34.6k/s · 命中 ~125.0k/s · 输出 ~80.2/s`；
     - 同步更新 ToolTip 与设置窗口中对于“未命中 Prefill 速度”与“命中 Prefill 速度”的解释文案。
  2. **时序采样门限调优 (`UsageRepository.AddSpeedSamples`)**:
     - 调整未命中与命中 Token 采样的有效占比门限，使其更好地覆盖交互式多轮对话中的混合输入轮次。
  3. **单元测试与发布**:
     - 更新 `TokenSpeedEstimateTests`，全套 103 项测试 100% 通过，自动发布 Release。

## 2026-08-30

### [FIX/UI] 固化主面板数据网格基准列宽并彻底解决配置重置问题
- **变更时间**: 2026-08-30 03:45
- **变更背景与根因定位**:
  - 用户反馈主面板中之前记忆的各个表格栏位宽度偶尔又被重置，无法确定触发节点；
  - 经系统排查定位核心致命触发节点：后台每 7 天自动调度全量扫描（或用户在设置中点击全量重新读取）完成更新 `LastFullScanUtc` 时，`RefreshCoordinator` 直接使用了启动时保留的陈旧 `_settings` 实例执行 `_settingsStore.Save(_settings)` 覆盖写入磁盘，导致用户在界面上调整好的最新 `ModelColumnWidths` 和 `ProjectColumnWidths` 被后台静默覆盖重置；
  - 次要触发节点：未监听网格的 `ColumnWidthChanged` 事件，且托盘右键退出时直接 Dispose 窗体未能触发 `FormClosing` 保存。
- **技术实现与优化**:
  1. **固化基准默认列宽 (`MainForm.DefaultModelColumnWidths` & `DefaultProjectColumnWidths`)**:
     - 将用户当前的列宽配置固化为系统默认初始宽度（按模型：模型 402、Provider 215、Input未命中 293、Cache Read 195、缓存命中率 212、预估速率 680、Output 260、API等值 155；按项目：项目 307、Provider 207、Tokens 175、Input未命中 287、Cache Read 220、缓存命中率 216、预估速率 565、Output 185、API等值 178）。
  2. **配置持久化原子安全更新 (`AppSettingsStore.Update` & `RefreshCoordinator`)**:
     - 在 `AppSettingsStore` 中引入线程安全文件锁与 `Update(Action<AppSettings> mutator)` 方法；
     - `RefreshCoordinator` 在全量扫描完成记录 `LastFullScanUtc` 或更新设置时，统一采用增量安全合并模式，彻底杜绝陈旧对象覆盖磁盘列宽与窗口大小等已有配置。
  3. **实时防抖持久化与托盘退出防护 (`WindowGeometryPersistence` & `TrayApplicationContext`)**:
     - 为数据表格挂载 `ColumnWidthChanged` 事件，配合 500ms 防抖计时器在用户拖动列宽后自动落盘保存；
     - 在托盘右键“退出”销毁窗体前，显式触发 `WindowGeometryPersistence.Save` 确保退出前最新状态安全落盘。
  4. **测试与交付**:
     - 增加 `AppSettingsStore.Update` 防覆盖与数据完整性单元测试，全套 103 项单元测试 100% 通过；已执行 Release 自动交付发布。

---

## 2026-08-28

### [FEAT/UI] 主页面各数据表格（按模型、按项目）支持栏位宽度自由拖拽与持久化记忆
- **变更时间**: 2026-08-28 20:18
- **变更背景与需求**:
  - 用户反馈希望主页面的各个表格栏位宽度支持自由拖拽调整，并且在退出程序后再次打开时依然保持调整后的宽度一致。
- **技术实现与优化**:
  1. **网格列宽调整模式改造 (`MainForm.CreateGrid`)**:
     - 将 `AutoSizeColumnsMode` 调整为 `None`，启用 `AllowUserToResizeColumns = true` 和每列 `Resizable = True`，为各列配置合理的初始基准宽度；
     - 每列设置安全最小宽度（`MinimumWidth = 40`），防止用户过度收缩导致列丢失。
  2. **配置持久化扩展 (`AppSettings` & `AppSettingsStore`)**:
     - 在 `AppSettings` 中增加 `ModelColumnWidths` 与 `ProjectColumnWidths` 字典，存储各列名与其自定义像素宽度；
     - 在 `Normalize()` 中对列宽进行范围安全钳位（30px ~ 2000px）。
  3. **生命周期自动绑定与恢复 (`WindowGeometryPersistence`)**:
     - 窗口创建初始化时自动从 `AppSettings` 还原「按模型」和「按项目」网格中各列的宽度；
     - 在窗口调整大小（`ResizeEnd`）、窗口关闭（`FormClosing`）以及隐藏到托盘（`VisibleChanged`）时自动捕获当前列宽并持久化写入 `settings.json`。
  4. **测试与发布**:
     - 新增 `AppSettingsTests.ColumnWidthsAreClampedAndPreservedAcrossSerialization` 测试用例；全套 102 项单元测试 100% 通过；执行 Release 交付发布。

### [FEAT/UI] 支持基于会话时间戳反推 Token 速率（区分未命中 Prefill、缓存读取与输出解码），并在主面板呈现与设置说明
- **变更时间**: 2026-08-28 17:00
- **变更背景与需求**:
  - 用户提出希望通过 session 记录反推大模型的 prefill 速度和输出 token 速度；
  - 核心要求：在主面板（`MainForm`）中展示且紧跟在“命中率”后方；必须在理论上清晰区分**未命中 Prefill** 与 **缓存读取（Cache Read）**；在设置窗口中说明反推原理并讲清数值受网络与排队影响通常低估；另外两个小面板（如悬浮额度面板等）保持纯净不显示。
- **技术实现与优化**:
  1. **速率计算模型与格式化器 (`TokenSpeedEstimate`)**:
     - 新增 `UsageTray.Core.TokenSpeedEstimate` 记录模型，支持存储 `UncachedPrefillTokensPerSecond`、`CacheReadTokensPerSecond`、`OutputTokensPerSecond` 以及各类样本数；
     - 提供 `ToShortDisplayString()`（如 `Prefill(未命中) ~3.8k/s · 输出 ~56.4/s`）与 `ToDetailedTooltip()` 丰富悬停明细。
  2. **仓储与时序抽样差分算法 (`UsageRepository.GetTokenSpeedEstimate`)**:
     - 遍历 `codex_snapshots` 与 `antigravity_generations`，按同一会话的时间戳差分 $\Delta t$ 提取连续请求样本；
     - 自动剔除异常区间（如 $<0.5\text{s}$ 或 $>300\text{s}$ 的长时间空闲）；
     - 严格区分 Prompt 结构：未命中输入占比高的样本参与“未命中 Prefill 速度”计算，高缓存命中率样本参与“缓存读取速度”计算；
     - 采用去离群值截尾平均算法（`CalculateTrimmedMean`）消除偶发网络抖动。
  3. **界面呈现与排版优化 (`MainForm` & `SettingsForm`)**:
     - `MainForm` 顶层指标栏：修复了 `TableLayoutPanel` 自适应标签被折叠截断（显示为“未命/缓存/命中/预估”）的排版问题，所有标题标签（`未命中：`、`缓存创建：`、`命中率：`、`预估速率：`）完整自适应呈现；
     - `MainForm` 数据网格：在「按模型」与「按项目」表格中，紧跟在“缓存命中率”后方新增独立的“预估速率”列，直观呈现每个具体模型与项目的独立速率；
     - `SettingsForm`: 增加独立“速率反推原理说明”模块，详细向用户解释基于时间戳差分与 Token 增量的反推原理，并阐明客户端端到端耗时包含网络 RTT、TLS 握手、服务端排队与客户端缓冲，因此数值通常低于纯硬件物理速度。
     - `QuotaPopupForm` 等小面板 100% 隔离，保持原样。
  4. **测试与发布**:
     - 新增 `TokenSpeedEstimateTests` 覆盖模型格式化、Codex 与 Antigravity 数据计算及快照聚合；全套 101 项单元测试 100% 通过；已执行 Release 自动发布。

### [FEAT/UI] 优化 Codex 额度过期智能推断（乐观回满）与多池解耦机制，彻底解决备用通道导致的显示滞后
- **变更时间**: 2026-08-28 14:22
- **背景与根因**:
  - 用户反馈当长会话在之前耗尽 5 小时额度并切入 `gpt-reserve` 备用池后，即使时间已过重置点（主账户已回满 100%），只要用户仍在当前会话中交互，OpenAI 服务端流式响应中就只有 `gpt-reserve` 的周配额，`secondary`（主周额度）物理上为 `null` 且完全不下发主池 5 小时数据。这导致本地日志在 Reserve 期间缺少主池事件，统计器 5h 快照停留在切入前的旧数据上。
- **优化与升级**:
  1. **乐观时间推断回满机制 (`QuotaSnapshot.EffectiveRemainingFraction`)**:
     - 在 `QuotaSnapshot` 中增加 `EffectiveRemainingFraction()` 方法。
     - 当检测到 5 小时或周额度快照的 `ResetAt` 时间已过且记录为 0% 时，系统智能推断当前周期已自然走完，自动按 `100% 剩余 (推断已重置)` 进行满额呈现；
     - 托盘简要提示（`BuildCompactText`）与弹窗气泡（`FormatRemaining`）同步展示 100% 满额状态（消除旧快照 0% 带来的误导）；
     - 当后续有常规主模型交互下发最新的云端真实数值时，平滑自动校准覆盖。
  2. **多池生命周期完全独立管理**:
     - `codex-reserve` 与主模型的 `codex-weekly` 彻底独立插槽，Reserve 周配额变动绝不污染或覆盖标准周配额。
  3. **UI 与引导说明升级**:
     - 在自绘额度面板（`QuotaSummaryControl`）与悬浮窗（`QuotaPopupForm`）中，过期快照呈现绿色 `100% (推断已重置)`；
     - 底部说明明确更新为：“• Codex 额度通过 Session 对话记录提取；若额度已过重置时间，系统自动按周期推断为满额，在 Codex 中发送一次常规模型消息即可校准云端精确快照。”
  4. **全量测试与本地发布**:
     - 更新对应单元测试用例，全套 96 项单元测试 100% 通过，并已成功 Release 发布。


---

## 2026-08-27


### [FEAT] 支持 GPT Reserve 独立额度、Luna 定价与 Codex 双额度双周期统计体系（ParserVersion 5 / SchemaVersion 4）
- **变更时间**: 2026-08-27 20:50
- **变更详情**:
  1. **GPT Reserve 定价规则支持**:
     - 在内置计价体系（`default-pricing.json` 及 `PricingService`）中新增 `gpt-reserve*` 规则，按 GPT-5.6 Luna 官方定价标准计算（Input \$0.2/M，CacheRead \$0.02/M，CacheWrite \$0.25/M，Output \$1.2/M），并配置 >272K Tokens 的长上下文加价阶梯（Input \$0.4/M，CacheRead \$0.04/M，CacheWrite \$0.5/M，Output \$1.8/M）。
  2. **Codex 解析器升级与 Reserve 配额识别（ParserVersion 5）**:
     - 升级 `CodexJsonlParser.ParserVersion` 至 5。
     - 在 `ExtractRateLimits` 中支持识别 `gpt-reserve` 运行时的独立周配额事件（`window_minutes: 10080`，`secondary: null`），提取为 `ModelOrPoolId = "codex-reserve"`，`DisplayLabel = "Codex Reserve"`。
     - 支持同一 Session 文件中标准额度与 Reserve 独立额度快照的共存与按 Pool 保存。
  3. **数据库迁移与重置时间解耦（SchemaVersion 4）**:
     - 升级 `DatabaseMigrations.CurrentVersion` 至 4，自动清理旧版解析器残留的被 Reserve 污染的错误 `codex-weekly` 快照。
     - 消除标准周额度重置时间（`09-02 03:16`）与 Reserve 备用周额度重置时间（`09-03 19:17`）的时间同步错位问题，彻底解决因窗口截断导致本周标准模型实际消耗少统计的问题。
  4. **周订阅统计与满额预估双池独立展示**:
     - 在「额度与用量摘要」面板（`QuotaSummaryControl`）与悬浮窗（`QuotaPopupForm`）中，周订阅统计与预估区域同时分别展示 **Codex 标准额度** 与 **Codex Reserve 备用额度** 各自的本轮周消耗与周满额预估（$\text{EstimatedWeeklyCost} = \frac{\text{周消耗金额}}{\text{已用比例}}$），并分别标注各自独立的周重置时间与倒计时。
     - 主界面顶部卡片展示 `Codex: $标准 | $Reserve` 及分池周重置时间（`标准: ... | Reserve: ...`）。
     - 托盘紧凑单行 Tooltip（`QuotaDisplayFormatter`）同步支持双额度紧凑模式（如 `Codex 62% (备用 76%)`）。
  5. **单元测试与验证**:
     - 新增 `CodexReserveQuotaTests` 单元测试类，覆盖定价匹配、长上下文、解析器多池提取、双周期聚合预估及 Tooltip 紧凑格式化。
     - 全套 94 项单元测试 100% 通过。

---

## 2026-08-26

### [UI/FEAT] 额度面板重置时间增加“X天X小时X分后”相对倒计时显示
- **变更时间**: 2026-08-26 22:30
- **变更详情**:
  1. **新增 TimeFormatter 相对时间工具**: 新建 `UsageTray.UI.TimeFormatter`，将未来目标时间差精确格式化为“X天X小时X分后”、“X小时X分后”、“X分后”、“不到1分钟后”或“即将重置/已重置”，并提供统一的标准重置时间格式化方法 `FormatResetWithRelative`。
  2. **全面升级额度与用量摘要面板**: 在托盘悬浮窗（`QuotaPopupForm`）与主窗口额度自绘面板（`QuotaSummaryControl`）中，为 Antigravity 与 Codex 的 5 小时窗口、周窗口配额行以及周订阅预估本轮消耗行追加直观的相对时间倒计时（如 `（重置 08-27 02:59，4小时50分后）` 或 `（重置 09-02 02:34，6天4小时29分后）`）。
  3. **主界面与文本格式化同步增强**: 主窗口顶部 API 等值卡片的周期说明标签（`_codexCycleNote`、`_antigravityCycleNote`）以及托盘纯文本弹出层（`QuotaDisplayFormatter`）同步追加相对时间倒计时；适当微调浮窗基准宽度至 580px，保证在各分辨率及长模型名称下排版呼吸自然。
  4. **单元测试扩充与验证**: 新增 `TimeFormatterTests` 单元测试类（涵盖跨天、整天、整小时、小于1分钟及过期边界用例），全量 89 项单元测试全部通过。

---

## 2026-08-24

### [DOC/SPEC] 建立系统文档体系规范与代码导航索引
- **变更时间**: 2026-08-24 17:40
- **变更详情**:
  1. **建立独立历史记录规范**: 将原 `项目文档.md` 中庞大的版本更新日志独立剥离至 `历史记录.md`，使 `项目文档.md` 聚焦于系统架构、核心规则与功能设计。
  2. **新增系统代码导航索引**: 按照 `AGENTS.md` 规范新建 [`CODE_INDEX.md`](CODE_INDEX.md)，建立工程结构、命名空间、类职责清单、调用链路图与核心计价公式的完整索引体系。
  3. **精炼重构核心文档**: 重构 [`项目文档.md`](项目文档.md)，建立标准化文档体系链接，明确项目定位、数据来源与口径、可信度规则、高 DPI 交互与性能架构。
  4. **工程验证与本地发布**: 执行全量 78 项单元测试并通过，完成 Release 编译与本地发布产物（`publish`）自动同步。

---

## 2026-08-20

### [FIX] 修复 Codex 子任务与主会话交替导致的 Token 雪崩式放大缺陷（ParserVersion 4）
- **根因定位**: Codex 派生代码审查或子代理任务时（如 `codex-auto-review`），其生成独立的 `rollout-*.jsonl` 会话文件，首行 `session_meta.payload` 中 `session_id` 记录父级会话 ID，而 `id` 才是当前子会话自身的独立 UUID。旧版解析器在 `FindSessionId` 中未按名称优先级提取，导致子任务文件被错误标记为主会话同一 `SessionId`；当高 Token 主会话（约 1 亿 Token）与低 Token 子审查（约 200 万 Token）在时间轴上交错运行时，全局累计差分算法在每次交替中将整条历史 Token 重复累加，产生 140+ 亿的离谱放大。
- **解析器与属性提取器重构**:
  1. 重构 `JsonValueReader.TryGetProperty` 与 `FindConversationId`，严格按照调用方传入候选名称的优先级顺序匹配属性（优先匹配 `id`，再匹配 `session_id`）。
  2. 重构 `CodexJsonlParser.FindSessionId`，优先提取 `session_meta.payload.id` 作为物理文件的专属逻辑账本，确保子任务与主会话完全隔离。
  3. 升级 `CodexJsonlParser.ParserVersion` 至 Version 4，触发数据库旧缓存自动失效并执行全量归一化重算。
- **测试与验证**: 新增子任务 `session_meta` 独立 ID 提取及主/子会话交错零膨胀单元测试，全部 78 项测试通过。

### [UI/STYLE] 图标透明图层与圆角边缘抗锯齿去白边修复
- **原因排查**: 截图工具截取屏幕上的圆角图标时会将屏幕底色合并为不透明白色（`#FFFFFF`，`Alpha=255`），导致直接转为图标后在深色任务栏或托盘中呈现带白色矩形方框的视觉缺陷。
- **透明通道精确重建与平滑抗锯齿**: 针对四个外角执行 BFS 洪水填充，将外围不透明白色背景置为 100% 透明（`Alpha=0`）；对圆角边缘过渡像素（27-28px）执行亮度距离衰减与 Alpha 平滑羽化（Defringe），彻底消除白边与锯齿感。
- **多分辨率重生成与本地发布**: 重新生成包含 16x16 至 256x256 完整尺寸的 `app.ico` 与 32bpp ARGB `app.png` 并更新发布产物。

### [PERF] 内存深度优化与每周全量扫描机制
- **启动扫描策略与每周校准**: 程序启动改为默认增量扫描（毫秒级比对完成）；在 `AppSettings` 中记录 `LastFullScanUtc`，仅在距上次全量扫描超过 7 天（或首次启动）时在后台静默执行一次全量校准并更新时间戳。
- **设置窗口交互升级**: 更新全量扫描说明文字并动态展示上次全量扫描时间（`yyyy-MM-dd HH:mm`）；用户手动点击“全量重新读取”完成后更新时间戳并保留完成提示弹窗。
- **Codex 增量刷新短路与巨型审计 JSON 瘦身**: 在 `CodexProvider` 中实现 0 变更短路，无会话文件变动时直接跳过全量快照查询与归一化重排；移除日常刷新中将全量数万条事件序列化为巨型 JSON 存库的行为，彻底消除 LOH（大对象堆）内存膨胀。
- **Antigravity Protobuf 零分配流式解码**: 重构 `AntigravityProtobufReader`，引入基于 `ref struct` / `ReadOnlySpan<byte>` 的流式按需扫描器 `ProtobufSpanReader`，彻底消除每次解析产生的大量 `List<RawProtobufField>` 托管对象与堆垃圾。
- **内存修剪与工作集自动回收（MemoryOptimizer）**: 封装 `MemoryOptimizer.TrimMemory()`，在全量或后台刷新后执行 LOH 堆压缩、强制 GC 及 Windows 原生 `SetProcessWorkingSetSize`（EmptyWorkingSet），后台常驻内存大幅降低至 15MB~30MB。
- **SQLite 缓存限制**: 在 `UsageDatabase` 及会话库连接中配置 `PRAGMA cache_size = -2000`（限制 2MB 缓存）。
- **单元测试套件扩充**: 新增 `LastFullScanUtc` 序列化与过期判定、`ProtobufSpanReader` 解码及 `MemoryOptimizer` 安全执行单元测试，全部 76 项测试通过。

### [ASSET] 引入专属应用图标资源并全面替换程序 Windows 图标、窗体图标与系统托盘图标
- **图标资源导入与多分辨率 ICO 生成**: 将指定图标导入项目 `src/UsageTray/Resources/app.png`，并自动生成支持 16x16、24x24、32x32、48x48、64x64、128x128、256x256 的标准多分辨率 Windows 图标文件 `app.ico`。
- **可执行文件原生应用图标集成**: 在 `UsageTray.csproj` 中配置 `<ApplicationIcon>` 与 `<EmbeddedResource>`，使生成的 `UsageTray.exe` 在 Windows 资源管理器与任务管理器中直接展示高清原生图标。
- **窗体与托盘图标统一加载**: 重构 `AppIcon.cs`，优先从内嵌资源中动态提取适配尺寸的图标（并保留矢量几何兜底绘制逻辑），全面同步更新 `MainForm`、`QuotaPopupForm`、`SettingsForm`、`PricingViewerForm`、`DateRangeDialog` 窗口图标及系统托盘 `NotifyIcon` 托盘图标。

### [UI/FIX] 修复 Codex 5 小时限额显示与主面板额度面板全面升级
- **Codex 额度采样时间隔离与历史废弃窗口清除**: 重构 `UsageRepository.GetLatestQuotas` 查询，按提供商的最新采样时间戳（`captured_at_utc = MAX(captured_at_utc)`）严格筛选当前有效快照批次，杜绝历史数据库遗留的过期 5 小时记录与旧重置日期泄漏；`CodexJsonlParser` 改为单次最新 `rate_limits` 事件提取，避免多事件旧窗口累加。
- **动态窗口自适应渲染**: 当 Codex 当前仅包含周限额（无 5 小时限额）时，悬浮窗与摘要格式化器不再输出 5 小时行及占位符，仅清晰呈现周额度与周订阅预估。
- **主面板额度选项卡自绘升级**: 新增通用的 `QuotaSummaryControl` 控件，将主面板原先的纯静态说明文字升级为与悬浮窗（`QuotaPopupForm`）100% 一致的高清彩色自绘面板（支持 Antigravity 双模型池配额、Codex 周配额、周消耗与满额预估及自适应滚动），在主窗口中获得一致且丰富的额度监控体验。

### [UI/FEAT] 主面板 API 等值卡片支持 Antigravity 双模型池金额分项展示
- **双额度拆分呈现**: 主面板顶部 API 等值卡片中的 Antigravity 行升级为 `Antigravity: $Gemini | $Claude` 双额度格式（如 `Antigravity: $22.23 | $0.00`），前面代表谷歌自研 Gemini 模型消耗，后面代表 Claude / 3P 模型消耗，无需额外冗余文字说明，与下方的 `周期：Gemini: ... | Claude: ...` 严格一一对应。

### [CORE] 统一 Antigravity 多模型池周额度周期与金额口径
- **多池独立周期聚合**: 在 `UsageRepository.GetAntigravityUsageInPoolWindows` 与 `UsageAggregator` 中，严格按照 Antigravity 各模型池的各自有效周窗口（Gemini: $T_{reset} - 7\text{天}$ 即 08-19 起；Claude/GPT: $T_{reset} - 7\text{天}$ 即 08-17 起）独立筛选 Token，杜绝将 Gemini 在 08-19 上次重置前产生的旧用量混入本轮周额度。
- **全界面口径一致**: 主面板「本次周额度」总金额、模型明细表格与托盘摘要弹窗「Gemini 本轮消耗」全面实现完全一致的精准统计。

### [FIX] 修复刷新合并警告时丢弃 Antigravity 周估算指标的缺陷
- **根因修复**: `RefreshCoordinator.CopyWithWarnings` 原先手动构造 `DashboardSnapshot` 对象时遗漏了 `AntigravityEstimates`、`AntigravityApiEquivalentUsd` 及 `CodexApiEquivalentUsd` 字段，导致刷新过程产生任意 Warning 时（如 Provider 离线提示），快照中的 Antigravity 周预估对象被置空，弹窗回退为默认的 `$0.00`。
- **记录化升级**: 将 `DashboardSnapshot` 定义重构为 `sealed record`，使用 `snapshot with { Warnings = ... }` 非破坏性复制，彻底杜绝后续扩展字段被意外漏拷的问题。

### [CORE/PRICING] Antigravity 额度统计全面对齐与长上下文/定价规则完善
- **长上下文加价判定与透传**: 在 `AntigravitySqliteHistoryParser` 与 `AntigravityQuotaEstimator` 中，严格按单次 Request 判定长上下文阈值（如 Gemini 3.1/2.5 Pro 超 200K Tokens，GPT-5.6 超 272K Tokens），准确聚合 `LongContextInputTokens`、`LongContextCachedInputTokens`、`LongContextOutputTokens` 并应用阶梯单价。
- **模型长上下文定价规则完善**: 在 `default-pricing.json` 及 `PricingService` 中明确界定不同模型的长上下文加价规则（Gemini Pro 系列有阶梯加价；Gemini Flash 系列在 1M 上下文内平价；Claude 全系列平价无长上下文阶梯；GPT-5.6 系列有阶梯加价），补充 `gemini-2.5-pro*` 长上下文加价规则（输入 $2.50，缓存读 $0.625，输出 $15.00，阈值 200K）。
- **已物理删除会话源自动清理**: 在 `AntigravityProvider` 刷新流程中对比磁盘扫描列表与数据库记录，自动删除已不存在的 `.db` 源记录，防止已删除会话在用量与周周期聚合中残留。
- **解析器版本（ParserVersion）失效机制升级**: 升级 Antigravity 解析器版本至 Version 3，当解析逻辑升级后自动标记旧缓存失效并重新计算指标。
- **单元测试与回归套件**: 新增 Antigravity 单次长上下文计价测试与删除源自动清理测试，全部 62 项单元测试通过。

### [UI/LAYOUT] 精简卡片说明行与 Antigravity 排序预估体验升级
- 移除主面板「API 等值」卡片底部冗余的说明文字，彻底释放卡片纵向空间；在「额度与用量摘要」弹窗中，Antigravity 明确按「先 Gemini、后 Claude」顺序排布，并与 Codex 保持高度一致展示周订阅本轮消耗与满额预估。

### [FEAT] 分应用独立周期展示与周额度按项目统计支持
- 在主面板「API 等值」卡片中，将 Codex 和 Antigravity 的周期分别独立放置在各自金额下方，Antigravity 明确展示 Gemini（`08-19~08-26`）与 Claude（`08-17~08-24`）双周期；修复 Codex 周额度聚合漏带 `ProjectKey` 的问题，使周额度模式下「按项目」Tab 完整准确统计各项目消耗。

### [FIX] 额度弹窗 Antigravity 分池去重与周预估过滤修复
- 过滤周订阅统计预估中的 5 小时窗口误混入项，严格只展示真正的 `weekly` 周窗口；增加泛指项（`Weekly Limit Remaining` / `Five Hour Limit Remaining`）智能过滤去重，精准独立展示 Gemini Models 与 Claude/GPT models 的周额度与预估。

### [UI] API 等值卡片高度自适应与布局优化
- 卡片数值区高度扩展为自适应并列双行布局（`TableLayoutPanel`），彻底解决高分屏或特定系统字体下 `Antigravity` 数值被截断遮挡的问题；优化缓存指标行文字标签（`未命中 / 创建 / 命中率`），杜绝折行截断。

### [UI] 主面板默认显示本次周额度与分应用 API 等值展示
- 主面板日期下拉框默认选中「本次周额度」；顶部 API 等值卡片加宽至 35%，同时并列展示 `Codex: $XX.XX` 与 `Antigravity: $XX.XX` 各自的独立等值金额，避免笼统合并。

### [UI/INTERACT] 额度摘要面板拖拽锁定与防失焦关闭
- 优化 `QuotaPopupForm` 交互行为，用户按住拖拽移动面板后自动进入锁定状态，点击屏幕其他区域不会失焦关闭，仅在直接点击面板或再次点击托盘图标时才关闭。

### [FEAT] 日期范围新增“全部”选项与缓存命中率统计
- 主面板日期下拉框新增“全部”周期选项（`DateRange.AllTime()`）；主面板指标卡缓存行、按模型表、按项目表全量增加精确到小数点后 2 位的“缓存命中率”（$\text{Cache Hit Rate} = \frac{\text{CacheReadTokens}}{\text{TotalInputTokens}} \times 100\%$）统计列与指标。
- 移除额度与用量摘要弹窗中的“本地历史会话用量”区块，聚焦于 5h / Weekly 实时配额剩余；重构 Antigravity 周周期估算引擎（`AntigravityQuotaEstimator`），与 Codex 完全统一，基于当前有效周窗口（`cycleStart ~ resetAt`）精准聚合计算各模型池的本轮周消耗与满额预估价值。

### [UI/FEAT] 本次周额度周期起止时间展示
- 日期选择为“本次周额度”时，在 API 等值卡片下方与状态栏清晰呈现该轮订阅周期的起止时间范围（`yyyy-MM-dd HH:mm ~ yyyy-MM-dd HH:mm`）。

### [FEAT/FIX] 重构设置窗口布局与全面修复 Antigravity 模型价格体系
1. **设置窗口自由缩放与尺寸记忆**: 切换为 `AutoScaleMode.Font`，`SettingsForm` 升级为 `FormBorderStyle.Sizable`，支持右上角最大化及鼠标任意拉伸调整大小；初始尺寸扩大为 `680 x 560`（最小 `560 x 460`），并在 `AppSettings` 中实现窗口宽高记忆，彻底解决高分屏下的文字与控件截断问题。
2. **新增独立模型定价规则面板**: 在设置窗口新增“模型定价”板块与“查看当前所有模型价格”按钮，点击弹出专用的独立规则查看面板（`PricingViewerForm`），支持按 Provider 筛选与模型名称实时搜索，直观呈现所有已配置的单价与长上下文阶梯。
3. **Antigravity 全模型价格与别名覆盖**: 全面核实并补充 Claude 系列（`claude-sonnet-4-6*`, `claude-opus-4-6*`, `claude-3-7-sonnet*` 等）、Gemini 系列别名（`gemini-pro-default*`, `gemini-3-flash-a*` 等）及开源模型（`gpt-oss-120b*`）定价规则。
4. **配置热升级与 DisplayName 智能推断**: `PricingService` 增加自动规则合并机制，老用户启动时自动升级并补齐新内置模型规则；当部分记录中的 `ResponseModel` 为空或 Unknown 时，自动根据 `DisplayName` 回退推断规范模型名称。
5. **Antigravity 实时额度探测与端口精准发现**: 增加基于 `language_server` 进程 PID 的毫秒级专属端口直接发现机制；增强 `AntigravityQuotaParser` 深度解析 `RetrieveUserQuotaSummary` 嵌套的 `groups` 和 `buckets` 结构。

### [CORE/DATA] 重构 Antigravity 数据源解析体系，正式接入 `conversations/*.db` Protobuf 解码引擎
1. 准确解析未命中输入 `input_tokens`、缓存读取 `cache_read_tokens`、缓存创建 `cache_write_tokens`、思考输出 `thinking_output_tokens`、响应输出 `response_output_tokens` 与总输出 `aggregate_output_tokens`；实测保持 `aggregate_output == thinking + response` 恒等式 0 错误。
2. 实现以 `response_id` 优先、`generation_id` 为辅的复合去重机制（`dedupe_key`），杜绝跨会话分叉导致的 token 重复计费。
3. 从 `steps.metadata` 提取毫秒/纳秒级 `google.protobuf.Timestamp`，并结合 `conversation_summaries.db` 与 `trajectory_metadata_blob` 自动解析并映射项目的规范绝对路径。
4. 建立 Antigravity 额度等值估算引擎（`AntigravityQuotaEstimator`）：按 Gemini Models 与 Claude/3P 分池，在有效 Quota Epoch 区间内聚合实际产生的 API 成本，推算完整 5h / Weekly 额度的 API 等值金额。
5. UI 与额度摘要面板全面升级：明确区分「API 已用等值」与「额度 API 等值估算」，托盘悬浮、摘要弹窗与诊断报告全面呈现真实 Antigravity Token 分布与推算指标。

### [FEAT] 主面板日期下拉列表新增“本次周额度”选项
- 完全脱离自然日整天统计，精确基于各 Provider 当前周配额订阅周期（$T_{start} = T_{reset} - 7\text{天}$ 至当前时间戳）聚合 Token、模型明细与等值金额，并在底部状态栏明确呈现起止时间范围。

### [UI] 规范托盘悬浮 Tooltip 分隔符与设置窗口自适应排版
- 规范托盘悬浮 Tooltip 分隔符（`|` 专用于分隔 APP，同一 APP 内部 5 小时与周额度使用 `/` 分隔）；全面重构设置窗口布局，移除冗余勾选框，采用左右分列操作按钮栏并自适应高 DPI。

### [UI/INTERACT] 额度与用量摘要弹窗支持拖拽移动与 Tooltip 优化
- 弹窗支持按住鼠标左键任意拖动位置、点击自动关闭；优化托盘悬浮紧凑 Tooltip（Codex 优先展示周额度，任一配额归零耗尽时动态展示重置刷新时间）；将“获取最新价格”收纳至设置窗口以精简主界面工具栏。

### [UI/FIX] 修复额度摘要多模型分组横向挤占与 Codex 重复快照
- 修复额度摘要多模型分组横向挤占导致的文本裁切问题，多模型分组自动分行独立缩进排版并自适应测量高度；修复 Codex 因历史跨会话残留导致显示重复 weekly/5h 额度快照的问题，按窗口类型绝对去重取最新。

### [UI/FEAT] 优化托盘交互与重构额度摘要面板
- 移除悬浮大面板，单击切换摘要，双击打开主面板并防冲突；重构额度摘要面板为加宽自绘多层级彩色排版并去除冗余状态栏；新增 Codex 周窗口订阅额度本轮金额统计与满额价值动态预估，并新增 3 个单元回归测试。

### [FIX] 修复设置窗口异步全量重读在窗口关闭时触发的 `ObjectDisposedException`
- 优化托盘紧凑文本与悬浮摘要，准确提取并优先展示最活跃/已消耗的 Antigravity 配额组（如 Gemini 97%）。

### [FIX] 修复 Antigravity local language_server 本地 quota 接口探测
- 兼容 HTTP/HTTPS 双协议与 Protobuf 载荷格式，支持 `RetrieveUserQuotaSummary` 嵌套配额组解析并新增回归测试。

### [PRICING/CORE] 补齐 GPT-5.6 Sol/Terra/Luna >272K 长上下文价格与服务档位维度
- 移除 Codex `gpt-5*` 广泛兜底；新增两个真实 Golden Session、独立参考解析器及回归测试。

### [CORE/REPAIR] Codex 用量完整修复规范重构
- 重构快照解析与逻辑会话归一化，统一全局累计计数器，支持模型切换/回退/移动去重/重叠分支压制/计数回退/缺失源保护，新增 SQLite 原始快照、schema 迁移、重建前后诊断和 CSV 审计。

---

## 2026-08-19

### [INIT] 项目初建与基础架构确立
- 按 V2 API Equivalent 规格从零建立 WinForms 托盘程序、Codex 增量统计、统一价格/成本层、Antigravity 历史与 status-line token 链路、local quota 解析、SQLite 缓存、仪表盘和验收测试。
- 补齐完整 JSON 历史文件解析，修正混合计价聚合的可信度传播，更新 GPT-5.6/GPT-5.4 与 Gemini 3.x 默认价格规则，并新增 3 个回归测试。
- 增加只读 SQLite 显式 token 字段解析和来源优先级测试；无法确认 schema 时继续保守降级。
- 重构高 DPI 主界面布局，增加官方价格页手动更新按钮、解析保护和 2 个价格更新回归测试。
- 修复高 DPI 下按钮、指标卡、图表标签、表头、数据行和状态栏的文字裁切，改为按实际字体高度动态留白和自动行高。
- 修复设置窗口高 DPI 裁切，增加自定义日期区间、托盘 5 小时/周额度及重置时间悬浮摘要，并更换为简洁几何图标。
- 修复 Antigravity quota 因缺少本地 CSRF token 导致的空显示；增加独立的“未命中缓存读取”统计行及回归测试。
- 将 API 等值的展示和输入拆分对齐 Sub2API token 口径，增加 Cache Creation 展示及兼容性回归测试。
- 托盘悬浮摘要增加 Codex 日志用量和订阅窗口未知状态；右键菜单期间暂停悬浮层，自动隐藏改为 3 秒，增加左键固定/取消固定与点击关闭，并记忆主窗口尺寸。
- 普通刷新改为增量模式，启动后台全量读取，设置中增加全量重新读取入口；刷新与范围整理移出 UI 线程并保持当前日期/Provider 选择；解析 Codex session 中可用的 `rate_limits`，显示 5 小时/周额度快照。
